# Build phase 4.15: the two-environment release flow

Branch: `phase/4.15-release-environments`
Depends on: 4.12, merged 2026-08-24 as PR #61 and PR #62; 4.14, merged 2026-08-26 as PR #68
Opened: 2026-08-27
Status: OPEN. Built and verified live; awaiting judge and adversary. TWO mid-phase redesigns by product-owner decision: the production branch renamed `main` to `production`, and the topology changed from two environments to two PROJECTS after F-4.15-03 measured that a Railway service's branch is service-level. Release automation (tag, changelog, back-merge) was added to scope on the same day.

Inserted by product-owner decision on 2026-08-24, the fourth such exception after 4.8, 4.10 and 4.11/4.12, and refined on 2026-08-27. Section 25 does not contain it. Section 24 specifies Railway's environment feature and requires "a separate variable set per environment, so a dev credential never reaches production", so the ENVIRONMENTS are a pull-forward. The BRANCH FLOW is genuinely new: Section 24's CD design names only `develop` and says phase branches never auto-deploy, so a release branch and a production branch are an addition to that model.

## Table of contents

- [What this phase is for](#what-this-phase-is-for)
- [Everything measured before any change was made](#everything-measured-before-any-change-was-made)
- [What the refinement settled](#what-the-refinement-settled)
- [Goal contract](#goal-contract)
- [Tickets](#tickets)
- [Coverage: what this phase does not cover](#coverage-what-this-phase-does-not-cover)
- [Findings](#findings)
- [History](#history)

## What this phase is for

Today there is one deployment. Build phase 4.12 wired CD to `develop`, so every merge to `develop` reaches the public URL that has been handed to people. There is no place to see a change running before the audience sees it, and build phase 4.14 made CI advisory rather than merge-blocking (F-4.14-A-04), so nothing structural stands between a merge and the demo.

The deliverable is two environments with separate URLs, separate variable sets and separate data, and a branch flow that promotes code from one to the other deliberately rather than on every merge.

## Everything measured before any change was made

Measured on `phase/4.15-release-environments` at its branch point, `9b4d1ad`, on 2026-08-27. This table is the input to the design rather than a summary of it, following build phase 4.14's practice, and two of its rows changed what the tickets say.

| What | Measured value |
|------|----------------|
| Railway project | `system3-search-agent`, id `0f85f78e-1ffc-4a62-b3d8-a03dcae9b585`, ONE project |
| Environments | exactly ONE, named `production`, id `e8dee09d-2991-48e7-ac46-998b7e3f9b80` |
| Services | `search-agent-api`, `search-agent-web`, `Postgres`, `Redis` |
| What `production` watches | branch `develop`, so the environment named production has never deployed a production branch |
| Branches on origin | `develop` and four phase or fix branches. NO `main`, so the production line does not exist yet |
| CI push trigger | `branches: [develop]` only |
| CI pull_request trigger | unfiltered, so a pull request into `main` ALREADY runs every gate with no change |
| `USER_DB_URL` host | `postgres.railway.internal`, the private-network name, which resolves per environment rather than globally |
| Variables set on `search-agent-api` | 21 non-Railway names |
| Variables set on `search-agent-web` | 4 non-Railway names |
| `railway environment new --duplicate` | available, and carries service configuration |

Two measurements changed the plan.

The CI trigger is HALF DONE ALREADY, and the half that is missing is not the half it looks like. `on: pull_request:` carries no branch filter, so a pull request targeting `main` already runs all ten gates today. Only the `push` trigger names `develop` exclusively, so what is missing is the post-merge run on `main`, not the pre-merge one. Writing the ticket from the assumption instead of the measurement would have added a redundant `pull_request` filter and left the real gap open.

`railway environment new --duplicate` decides HOW the second environment is built, and the reason is build phase 4.12's most expensive learning. `search-agent-web`'s Root Directory is a dashboard-only field: that phase deployed a second copy of the Python API into the static frontend service, and could not fix it from the CLI at all. Creating the develop services from scratch would mean setting that field again by hand. Duplicating carries it. So this phase duplicates and then OVERWRITES the variables that must differ, rather than building the environment up from nothing.

### A gap found while measuring, not predicted

`env.example` and the deployed service disagree in both directions, and one direction is a live defect.

| In `env.example`, NOT set on any service | Set on a service, NOT in `env.example` |
|---|---|
| `ANTHROPIC_API_KEY`, `OPENAI_API_KEY` (alternate providers, unused) | `RUN_MIGRATIONS_ON_STARTUP` |
| `GRAPH_PG_HOST/PORT/USER/PASSWORD/DBNAME` (superseded by `GRAPH_QUERY_URL` at build phase 4.11) | `NIXPACKS_NO_CACHE` |
| `LANGSMITH_API_KEY`, `POSTHOG_API_KEY` (build phases 5.0 and 5.1, unbuilt) | `NIXPACKS_NODE_VERSION` |
| `OPERATOR_USER_IDS` (optional), `PORT` (Railway auto-sets it) | |

The left column is all explainable. The right column is not, and `RUN_MIGRATIONS_ON_STARTUP` is the one that matters: it gates `alembic upgrade head`, which `railway.json` runs as the first half of the start command, and it is absent from the file this repository tells people to provision from. Build phase 4.14 recorded that "about thirty environment values the suite needs" were missing from a clean machine and fixed it by DIFFING `env.example` against the job rather than discovering them one failure at a time. This is the same defect one layer out: provisioning a second environment from `env.example` alone would silently omit the variable that decides whether migrations run. Filed as F-4.15-01 and closed by T-4.15-06.

## What the refinement settled

Settled 2026-08-27 and logged in `DECISIONS.md`. The board row carried these as open questions since 2026-08-24.

| Question | Answer | Who decided |
|---|---|---|
| Separate environment or separate project | A second environment in the existing project | Section 24, which already names the feature |
| Which environment keeps the live URL | The existing one stays production and keeps `search-agent-web-production.up.railway.app` | Lead, on the ground that the URL is already in circulation |
| Shared or separate Postgres and Redis | Separate. Develop gets its own | PRODUCT OWNER, 2026-08-27 |
| `AUTH_SECRET` | Generated per environment, never shared | Section 24 |
| `CORS_ORIGINS` | Per environment, naming that environment's own web URL | Section 24 plus the URLs |

The data split was the only part that spends money and the only part that was genuinely open. It was decided against sharing on two grounds this build has already measured rather than on principle: build phases 4.6 and 4.10 count `interactions` rows to enforce the daily caps, so shared data would spend production quota on test traffic and pollute what build phase 4.13's history rail renders; and `railway.json` starts with `alembic upgrade head`, so a shared database carries a wrong migration into production the moment it merges to develop, which is the same unguarded promotion build phase 4.14 exists to stop, one layer down.

## Goal contract

Written before the first change, per `.claude/rules/goal-contracts.md`.

Done when, all five:

1. Two Railway environments exist in `system3-search-agent`: `production`, unchanged in name and public URL, and `develop`, new. Each has its OWN Postgres and Redis, and its own `search-agent-api` and `search-agent-web` answering at distinct public hostnames.
2. `develop` the environment watches `develop` the branch. `production` the environment watches `main`.
3. `main` exists on origin, and CI runs on a push to it as well as on a pull request into it.
4. The release flow is written down in one document that is the source of truth for how a change reaches production.
5. Every arm of the premise gate passes live, and every arm is proven able to fail by a mutation harness.

Verify surface, immutable for the run:

- `tests/system_03_search_agent/tools/test_release_environments_premise.py`, live arms behind `RUN_PREMISE_GATE=1`, each stated below with what makes it fail.
- `tests/system_03_search_agent/tools/test_release_environments_mutation.py`, one case per arm, asserting the arm goes red when the property it guards is broken.
- `python tracker/check_doc_drift.py --check` reporting 0 stale, 0 structural.
- CI green on the pull request.

The arms, and what each one would catch:

| Arm | Property | Goes red when |
|---|---|---|
| P1 | Both environments answer, at DIFFERENT hostnames | one environment is down, or both resolve to one deployment |
| P2 | An account created on develop cannot sign in on production | the two share a user database |
| P3 | A bearer token minted by develop is REJECTED by production, and the converse | `AUTH_SECRET` is shared between environments |
| P4 | Each API admits its OWN web origin and refuses the other's | `CORS_ORIGINS` was copied rather than set per environment |
| P5 | The two environments report different `APP_ENV` | the duplicate carried production's app config unchanged |
| P6 | Every non-Railway variable name on production's API is also set on develop's | duplication or hand-provisioning dropped one |
| P7 | `env.example` names every non-Railway variable the API service is given | a variable is load-bearing in the deployment and undocumented (this is F-4.15-01) |
| P8 | CI's push trigger includes `main` | a merge to the production line runs no gates |

P3 is the arm that carries the most weight, and it is the reason the gate is behavioural rather than a variable comparison. Reading two `AUTH_SECRET` values and asserting they differ would require printing secrets and would still only prove the values differ today. Minting a token on one environment and watching the other reject it proves the isolation through the code path that actually depends on it.

P2 and P3 together are also the two arms that would go red on the exact configuration the product owner rejected, which is what makes them worth their cost: had the shared-database option been taken, this gate would have been unwritable rather than merely red.

Constraints, true throughout:

- No secret value is ever printed, logged, or written to a tracked file. Variable NAMES are read and compared; values are never rendered. Where a value must be compared, it is compared by behaviour, not by content.
- `production` keeps its name, its public URL and its data. Nothing in this phase migrates, drops or rewrites production data.
- Production is never left watching a branch that does not exist. `main` is created and pushed BEFORE production's trigger moves to it.
- No merge to `develop` during the window between creating the develop environment and pointing it at the branch.

Blocked-stop, any of:

- Railway refuses a second environment on the current plan. Report the plan requirement and its cost; do not work around it by reusing production.
- The duplicate carries production's database credentials as literals rather than resolving `postgres.railway.internal` per environment, so develop would read production data. Stop before starting any develop service.
- Repointing production's trigger would leave the live URL serving nothing.

## Tickets

| Ticket | Deliverable | Files | Status | Acceptance |
|---|---|---|---|---|
| T-4.15-01 | The premise gate, written FIRST against infrastructure that did not exist, so its live arms failed rather than skipped | `tests/.../test_release_environments_premise.py` | in-review | 9 arms. All 8 original arms failed at the branch point, each for its own reason |
| T-4.15-02 | The production line as a branch. RENAMED mid-phase from `main` to `production` by product-owner decision | git only | in-review | `origin/production` exists at `9b4d1ad`; `main` created and then deleted; `develop` remains the DEFAULT branch |
| T-4.15-03 | The develop deployment. REDESIGNED mid-phase from a second environment to a second PROJECT, per F-4.15-03 | Railway | in-review | `system3-search-agent-develop` with its own Postgres, volume, Redis, api and web, watching `develop` |
| T-4.15-04 | Per-deployment divergence: own `AUTH_SECRET`, `APP_ENV`, `CORS_ORIGINS`, `VITE_API_BASE_URL`, own generated domains | Railway | in-review | P2, P3, P4 and P6 pass live |
| T-4.15-05 | Branch triggers: develop project watches `develop`, production project watches `production` | Railway | in-review | Both read back their own branch. This is what two projects bought and one project could not do |
| T-4.15-06 | Close F-4.15-01: `env.example` documents the three deployment-only variables | `env.example` | in-review | P7 passes and goes red when one is removed |
| T-4.15-07 | CI on the production line | `.github/workflows/ci.yml` | in-review | P8 passes and goes red on all three ways the trigger can break |
| T-4.15-08 | The release-flow document | `docs/build/Release_flow.md` | in-review | Rewritten for two projects, the `production` branch name and the release automation |
| T-4.15-09 | The mutation harness | `tests/.../test_release_environments_mutation.py` | in-review | 14 cases, every arm proven red under its own mutation AND green under a healthy control |
| T-4.15-10 | `GET /health` reports `app_env`, so a deployment can be identified from outside. Additive per pattern 10 | `src/.../web_sse/app.py` | in-review | P5 passes once this branch reaches `develop`; it is the one arm that cannot pass before the merge |
| T-4.15-11 | Release automation: semver from Conventional Commits, CHANGELOG.md, annotated tag, GitHub Release, back-merge pull request. ADDED mid-phase at product-owner request | `.github/workflows/release.yml`, `.github/release/*.sh` | in-review | Version derivation and changelog generation both run against real history; P9 pins the no-shell and no-injection rules |
| T-4.15-12 | Retire the leftovers so nothing bills for idle infrastructure or points at a dead branch | Railway, git | in-review | `main` deleted; both leftover environments deleted; all four live surfaces answer 200 afterwards |

Order is not free here. T-4.15-01 comes first deliberately so the gate is written against absence and cannot be tuned to whatever the infrastructure happens to do. T-4.15-02 precedes T-4.15-05 because production must never be pointed at a branch that does not exist. T-4.15-06, T-4.15-07 and T-4.15-08 touch disjoint files and are independent of the Railway work.

## Coverage: what this phase does not cover

Stated in the phase file rather than left to be discovered, per `.claude/rules/goal-contracts.md`, "a verify surface must state its own coverage".

- The gate does NOT prove that a release cut from `develop` and merged to `main` actually reaches production. It proves the triggers are configured and that a deploy happened. The first real release is the proof, and it is out of this phase.
- The gate does NOT test rollback. Railway's rollback is redeploying a previous build from its history, which is a console action with no API surface exercised here.
- The gate does NOT prove the two Postgres instances hold different DATA at rest. P2 proves it through the auth path, which is the path that matters, and says nothing about the `interactions` or `runs` tables.
- The gate does NOT make CI merge-blocking on `main`. F-4.14-A-04 is unchanged by this phase: branch protection still needs GitHub Pro or a public repository, so a red check on a release pull request still sits beside a working Merge button. This phase adds a second place that is true rather than fixing it.
- P6 compares variable NAMES, never values. A variable present in both environments with production's value copied into develop passes P6. P3 and P4 cover the two cases where that would actually be dangerous; nothing covers the rest.
- Nothing here tests the graph credential per environment. Both environments point at the same read-only HTTPS graph query service, deliberately: Layer 1 is read-only at the connection level, so there is no develop-versus-production hazard to separate.

## Findings

| ID | Severity | What | State | Reason | Owner |
|---|---|---|---|---|---|
| F-4.15-01 | minor | `RUN_MIGRATIONS_ON_STARTUP` gates `alembic upgrade head` in `railway.json`'s start command and is absent from `env.example`, so provisioning a second environment from the documented file would silently omit it. `NIXPACKS_NO_CACHE` and `NIXPACKS_NODE_VERSION` are also set and undocumented, though both are builder hints rather than app config | open | Found by diffing `env.example` against the deployed service while measuring, not predicted by any ticket | T-4.15-06 |
| F-4.15-02 | major | `railway service source connect --environment production` set the branch for the service in BOTH environments, not just production. Immediately after running it against `search-agent-api/production`, the DEVELOP environment's same service also read `branch: main`. The CLI's own `-e/--environment` help says "Environment to use for RESOLVING the service", and its automation note says GitHub sources are "connected at the SERVICE level and create deployment triggers for matching project environments". So the flag never scoped the write. The Railway MCP's `connect-service-source` states the same thing plainly ("Applies to the service in all environments") and was rejected for that reason before the CLI was tried; the CLI turned out to behave identically while reading as if it did not. Consequence at the moment of discovery: both environments deploy from `main`, so a merge to `develop` would reach NOTHING. Not an outage, because `main` and `develop` are the same commit, which is the only reason this was cheap | open | Found by verifying BOTH environments immediately after the write rather than only the one targeted. Predicted as a risk before running it, which is why the verification was written to check both | lead |
| F-4.15-03 | CRITICAL | A Railway service's git source (repo plus branch) is SERVICE-level, not environment-level, so two environments in one project CANNOT watch different branches for the same service. This defeats the phase's central deliverable as designed. MEASURED, not inferred: after pointing `search-agent-api` at `main` for production only, both environments read `branch: main`, while `search-agent-web`, untouched, reads `branch: develop` in BOTH. So the axis of variation is the service, never the environment. Three write paths were tried and all three agree: `railway service source connect --environment production` (changed both), `railway environment edit --environment develop --service-config search-agent-api source.branch develop` (returned `No changes to apply`), and Railway's own agent, which stated plainly that per-environment branch triggers are not supported in its model. Railway's DOCUMENTATION contradicts this: the guide "Isolate staging from production" says "Each service in each environment has its own deployment trigger" and describes a staging environment auto-deploying from a staging branch. The likeliest reconciliation is that the setting is DASHBOARD-ONLY, which is not a new shape here: build phase 4.12 recorded the Root Directory field as exactly that, reachable from the console and not the CLI. ESCALATED to the product owner rather than worked around, because every workaround changes the architecture they approved | open | Found by verifying both environments after a write that targeted one. The measurement is what makes it a constraint rather than a CLI complaint | PRODUCT OWNER |
| F-4.15-04 | major | The variable-copy script's skip list did not skip. `SKIP="|$*|"` expands to `|A B C|`, space separated, while the test is `[[ "$SKIP" == *"|$name|"* ]]`, which needs pipe separators, so it never matched and all five per-environment variables were copied from production into the develop project: `APP_ENV`, `CORS_ORIGINS`, `AUTH_SECRET`, `USER_DB_URL`, `REDIS_URL`. The `AUTH_SECRET` copy is the serious one, since a shared signing key is the exact property P3 exists to forbid, and it was introduced by the tooling built to prevent it. `USER_DB_URL` was less dangerous than it looks and the reason is worth recording: the copied value names `postgres.railway.internal`, and Railway private networking is PROJECT scoped, so it resolves to the develop project's own Postgres rather than production's, and the copied credentials do not match it, so the failure mode is a refused connection rather than silent cross-project reads. Caught by reading the script's own output, which printed all five under `copied:` and nothing under `skipped:` | fixed | Self-inflicted, found immediately because the script was written to print what it copied AND what it skipped. Printing both is what made a silent no-op visible; printing only the copied list would have looked correct | lead |
| F-4.15-05 | major | `derive_version.sh` read the previous release with a bare `git describe --tags --abbrev=0`, which returns the nearest tag of ANY shape. This repository already carries `baseline/pre-drift-fix`, a non-semver tag, so the first run computed the version `vbaseline/pre-drift-fix.1.0` and would have created a tag and a GitHub Release under that name. The parse did not fail, it produced a confident wrong answer, because `IFS='.' read -r major minor patch` happily splits any string | fixed | Found by RUNNING the script against this repository's real tag list rather than reasoning about it. Reading it would not have found this: the code is correct for every repository whose tags are all semver, and the defect is a fact about the tag list, not about the logic | lead |
| F-4.15-A-01 | major | `derive_version.sh` matches a breaking change with `case "$line" in *"BREAKING CHANGE:"*)` against every line of every commit BODY in the range, so the phrase anywhere inside a sentence bumps MAJOR. Conventional Commits defines it as a FOOTER, meaning the token must begin a line. MEASURED in a throwaway repository: a single commit `docs: clarify that this is not a BREAKING CHANGE: for anyone` with body `Reviewers asked whether this was a BREAKING CHANGE: it is not.` produced `bump=major -> v2.0.0` from a `v1.2.3` baseline. Any commit that QUOTES a breaking-change footer (a revert, a changelog edit, a review discussion pasted into a body, this very phase file if it were ever committed as a body) permanently publishes a wrong major version and a wrong tag. Anyone who can open a pull request chooses that text | open | Established by running `.github/release/derive_version.sh` against a purpose-built git repository with that exact commit, not by reading | unassigned |
| F-4.15-A-02 | major | Commit-subject text is written VERBATIM into `CHANGELOG.md` and then into the published GitHub Release body, so a commit subject is a content-injection sink into a permanent public artifact even though it is not a shell-injection sink. MEASURED: subject `fix(web): [click me](https://evil.example) and <img src=x onerror=alert(1)>` produced the changelog line `- web: [click me](https://evil.example) and <img src=x onerror=alert(1)> (171cbc1)`. GitHub renders release notes as markdown, so the link is live and clickable under the project's own name. `release.yml`'s header comment reasons carefully about `${{ github.event }}` shell injection and concludes commit text is safe because it is "only matched against, never executed"; that is true of the SHELL and false of the ARTIFACT. Nothing escapes, strips or length-caps the subject anywhere in `write_changelog.sh` | open | Established by running `write_changelog.sh` in a throwaway repository against that commit and reading the generated file | unassigned |
| F-4.15-A-03 | major | The version and the changelog can contradict each other on the exact case the major bump exists for. `derive_version.sh` bumps MAJOR on a `BREAKING CHANGE:` footer, but `write_changelog.sh`'s Breaking changes group matches only the HEADER form `^[a-z]+(\([^)]*\))?!:`. So a legitimate `feat(api): x` carrying a `BREAKING CHANGE:` footer publishes `v2.0.0` whose changelog section has NO Breaking changes group and lists the commit under Features. MEASURED: a run that printed `bump=major -> v2.0.0` produced a section containing only Features, Fixes and Maintenance. The one thing a reader opens a major release to find is the one thing the generator cannot emit | open | Established by running both scripts in sequence in a throwaway repository and diffing what each concluded from the same commit range | unassigned |
| F-4.15-A-04 | minor | `write_changelog.sh` emits its group labels as bare paragraph text, not markdown headings: `emit_group ... "Features"` writes the literal line `Features`. The version line above it is a real `## ` heading, so a rendered release reads as one heading followed by undifferentiated prose lines and bullets. The phase claims the changelog is "grouped by Conventional Commit type"; the grouping exists in the file order and not in the rendered document | open | Established by rendering-relevant inspection of generated `CHANGELOG.md` output from a real run: the labels carry no `#` characters | unassigned |
| F-4.15-A-05 | minor | A `|` anywhere in a commit subject silently truncates the changelog line. `write_changelog.sh` reads `git log --format='%s|%h'` and splits with `subject="${line%%|*}"`, taking everything before the FIRST pipe. MEASURED: `feat(api): support a|b pipe separated inputs` became `- api: support a (02e3847)`. The sha survives, the meaning does not, and nothing warns. Same run also showed an empty subject `feat:` producing the empty bullet `-  (d4f3d28)` | open | Established by running `write_changelog.sh` against those two commits and reading the output | unassigned |
| F-4.15-A-06 | minor | `write_changelog.sh` reintroduces, at the bottom of the file, the exact defect its own comment says it fixed at the top. The `Full diff` block is `echo "Full diff: ..." >> "$section"` followed by a BARE `echo` with no redirect, so the blank line goes to stdout and never reaches the section. MEASURED across two consecutive releases in a throwaway repository: the file ends up with `Full diff: \`v2.0.0..v2.0.1\`` on line 14 and `## v2.0.0 (2026-08-28)` on line 15, no blank line between them. The comment 30 lines above reads "The first version of this counted `head -n 6` ... and produced a version heading glued to the prose. A structural split cannot drift". The structural split does not drift; the missing redirect glues the heading anyway, from the other end, on every release after the first | open | Established by running derive_version.sh and write_changelog.sh twice in sequence over a real two-release history and reading the numbered output | unassigned |
| F-4.15-A-07 | major | `write_changelog.sh` SILENTLY DROPS every commit whose type is not one of the eight it hardcodes, while `derive_version.sh` still counts that commit toward the release. MEASURED in a throwaway repository: a range of six commits produced a changelog section listing ONE. `revert: undo the auth change that broke sign-in`, `perf(api): cut p95 latency`, `build(deps): bump alembic`, `style: reformat` and `Fix: capitalised type` all vanished; only `feat(api): a real feature` survived. `perf`, `build`, `style` and `revert` are standard Conventional Commits types, and a dropped `revert` is precisely the line a reader opens a release to find. Nothing warns and nothing fails: the section renders as a well-formed, confidently incomplete release note. In the degenerate case where NO commit matches any group, `should_release` is still `true` and the GitHub Release body `gh release create` receives is a single `Full diff:` line, verified by piping the awk extraction through `od -c` | open | Established by running derive_version.sh then write_changelog.sh over a purpose-built six-commit range and diffing `git log` against the generated section | unassigned |
| F-4.15-A-08 | major | The numeric guard in `derive_version.sh` passes a leading-zero component, the arithmetic then fails, and `set -euo pipefail` DOES NOT ABORT, so the script exits 0 having emitted `should_release=true` with a version IDENTICAL to the previous tag. MEASURED against tag `v1.2.08`: stderr showed `line 72: 08: value too great for base (error token is "08")`, stdout showed `bump=patch -> v1.2.08`, exit status 0, and `$GITHUB_OUTPUT` contained `should_release=true` and `version=v1.2.08`. Bash arithmetic reads a leading zero as octal, and `08` is not octal. The `for part in ...` guard directly above tests `*[!0-9]*`, so `08` is all digits and passes the very check whose stated purpose is that "a version that is not three integers must stop the release rather than be coerced into one". The downstream consequence is worse than a wrong version: `write_changelog.sh` runs and `tag_and_release.sh` PUSHES the changelog commit to `production` before `git tag -a v1.2.08` fails on an already-existing tag, leaving production carrying a duplicate changelog section for a release that was never tagged, and the next run recomputing the same version again | open | Established by tagging a throwaway repository `v1.2.08` and running derive_version.sh, capturing exit status and the written GITHUB_OUTPUT | unassigned |
| F-4.15-A-09 | major | P5 is RED live right now and the phase file's stated remedy for it is wrong, so done-when 5 ("Every arm of the premise gate passes live") cannot be reached by merging this pull request. MEASURED over HTTP: `GET /health` returns exactly `{"status":"ok"}` on BOTH `search-agent-api-production` and `search-agent-api-develop`, with no `app_env` key, and P5 asserts `"app_env" in payload`, so it fails on the first environment it reaches. The cause is branch skew: `git rev-parse` shows `origin/production` and `origin/develop` are BOTH at `9b4d1ad`, the branch point, and neither contains T-4.15-10's change, which exists only on `5dea231` on this branch. T-4.15-10's acceptance says P5 "passes once this branch reaches `develop`; it is the one arm that cannot pass before the merge", but the PRODUCTION deployment tracks the `production` branch, which a merge to `develop` does not move. P5 therefore stays red after this pull request merges and only clears after a full release promotes develop to production, which the coverage statement itself places OUT of this phase. The arm is honest; the completion claim built on it is not | open | Established by curling `/health` on both live APIs and comparing `git show origin/production:` and `git show origin/develop:` against the working tree for the app_env change | unassigned |
| F-4.15-A-10 | major | P1's docstring asserts a control the arm does not contain. It reads "the hostnames are compared, and the service ids reported by the two deployments are compared too, because two hostnames can still front one service". The body compares `prod["api"] != dev["api"]` and `prod["web"] != dev["web"]`, which are two strings read out of the same fixture file, then asserts each `/health` returns 200 with `status == "ok"`. THERE IS NO SERVICE-ID COMPARISON ANYWHERE IN THE ARM, and no response field other than `status` is ever read. So the precise failure the docstring names as excluded, two hostnames fronting one deployment, leaves P1 GREEN: the fixture strings differ by construction, and one healthy service answers `ok` twice. This is the shape LEARNINGS.md records as F-2.1-J5-01, a confident comment sitting above code that never implements it, which is exactly where the next reader stops checking. P2 and P3 do establish separateness behaviourally, so the premise survives; the arm that is documented as proving it does not | open | Established by reading the arm against its own docstring and confirming by grep that no service id, `x-railway` header, or any `/health` field beyond `status` is referenced anywhere in the arm | unassigned |
| F-4.15-A-11 | major | NO arm can detect a recurrence of F-4.15-01, the finding this phase opened around. P7 is named "env.example documents every variable the deployment sets" but it NEVER READS A DEPLOYMENT: it compares `env.example` against `deployment_sets`, a 26-name set hardcoded in the test body and described in its own docstring as "the measured set from 2026-08-27". A variable added to a Railway service tomorrow and omitted from `env.example` leaves P7 green forever, because the hardcoded list is the only thing `env.example` is measured against and nothing keeps that list current. The docstring defers the live half to P6, but P6 compares production's variable NAMES against DEVELOP's, never against `env.example`, and asserts one direction only. So the live set and the documented set are never compared by anything, in either direction. The exact defect T-4.15-06 closed is undetectable the second time it happens. This hole is NOT in the phase's coverage statement, which discloses only that P6 compares names rather than values | open | Established by reading P6 and P7 together and confirming that `deployment_sets` is a literal in the test body with no live read, and that P6's comparison operands are production and develop rather than production and env.example | unassigned |
| F-4.15-A-12 | major | The back-merge pull request, the one automated pull request that reaches the DEFAULT branch, is the one pull request CI never runs on. `open_backmerge_pr.sh` calls `gh pr create` with `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`, and GitHub documents that events raised by the default `GITHUB_TOKEN` do not create workflow runs. So the `pull_request` event for `chore/back-merge-vX` into `develop` raises no CI run, P8's carefully unfiltered `pull_request` trigger never fires for it, and because build phase 4.14 left the gates ADVISORY (F-4.14-A-04) there is no required check to notice the absence either. The pull request arrives with an empty checks list, which reads as "nothing to report" rather than "nothing ran". The same token behaviour also means `tag_and_release.sh`'s `[skip ci]` marker is not, as its comment claims, "what makes the loop terminate": the push cannot trigger a workflow regardless. The loop guard that is actually load-bearing is undocumented, and the one that is documented is decorative | open | Established by reading the workflow's `permissions` and both scripts' `GH_TOKEN` wiring against GitHub's documented GITHUB_TOKEN event-suppression behaviour, and confirming no PAT or App token is configured anywhere in `release.yml` | unassigned |
| F-4.15-A-13 | major | P9 verifies that `release.yml` contains no shell and then never looks at the shell. Its two real checks are a whole-string equality on each `run:` body and a `github.event` sink scan over `${{ }}` expressions in the WORKFLOW; its only statement about `.github/release/*.sh` is that each file exists and has an executable bit. Nothing reads a line of any script. The 4.14 lesson was applied correctly to the workflow and moved one hundred percent of the release logic into four files the gate does not open, so `derive_version.sh` and `write_changelog.sh` can be rewritten arbitrarily with P9 green. This is not hypothetical: F-4.15-A-01 through F-4.15-A-08, every behavioural defect found in this phase, live entirely inside those four unexamined files, and each was found by EXECUTING a script rather than by any arm. The coverage statement does not disclose that the release automation has no behavioural gate at all, and T-4.15-11's acceptance claims "Version derivation and changelog generation both run against real history" while no committed test runs either | open | Established by reading P9 end to end and confirming the only script-directed assertions are `script.is_file()` and the executable-bit check, then confirming by grep that no test file in the repository invokes any script under `.github/release/` | unassigned |
| F-4.15-A-14 | CRITICAL | THE DEVELOP WEB APP CANNOT REACH ANY API. `VITE_API_BASE_URL` was never baked into the develop frontend build, so the shipped bundle carries the local development fallback `http://127.0.0.1:8000`, which in a visitor's browser means the visitor's OWN machine. MEASURED by downloading both live bundles and extracting every absolute URL: `search-agent-web-develop-2aeb.up.railway.app/assets/index-BJ5_J9_N.js` (394383 bytes) contains `http://127.0.0.1:8000` and NO `*.up.railway.app` API host at all, while the production bundle at the same path contains `https://search-agent-api-production.up.railway.app` and no localhost. `VITE_` variables are compile-time substitutions, so setting the variable after the build leaves the old bundle serving. The develop deployment therefore satisfies T-4.15-12's acceptance ("all four live surfaces answer 200") while being useless for the one thing this phase exists to provide, a place to see a change running before the audience does. NO ARM CATCHES IT: P1 compares the two `web` values as FIXTURE STRINGS and never issues a single HTTP request to either web URL, P4 tests CORS at the API, and nothing anywhere asserts that a web deployment points at its own API. T-4.15-04's acceptance claims per-deployment `VITE_API_BASE_URL` and cites P2, P3, P4 and P6 as its evidence, none of which observe the frontend | open | Established by fetching both live web roots, extracting the hashed asset path from each, downloading both JavaScript bundles and grepping every absolute URL out of each; the two bundles disagree on exactly this one host | unassigned |
| F-4.15-A-15 | major | Two of the nine arms have NO mutation case, so done-when 5 ("every arm is proven able to fail by a mutation harness") and T-4.15-09's acceptance ("14 cases, every arm proven red under its own mutation AND green under a healthy control") are both false as written. MEASURED by listing every arm named anywhere in `test_release_environments_mutation.py` against every `def test_p*` in the premise gate: P6 and P9 appear in neither the parametrized list nor any standalone case, and the parametrized list holds 10 cases rather than 14. P9 is the worse omission of the two: it is the only arm guarding the release automation, and it is therefore the one arm whose ability to fail was never demonstrated, in a phase whose release automation carries eight separately filed behavioural defects | open | Established by grepping every `test_p*` identifier out of both files and diffing the two sets | unassigned |
| F-4.15-A-16 | minor | P3 permanently writes a new account into the PRODUCTION user database on every run, with no teardown anywhere in the module. `_throwaway_credentials()` mints a fresh uuid per run so runs never collide, which also means every run leaves another row behind, unbounded. P2 writes only to develop; P3 signs up on BOTH deployments because it needs a token minted on each. The phase's own constraint reads "`production` keeps its name, its public URL and its data. Nothing in this phase migrates, drops or rewrites production data", which is literally satisfied and misses the case: the gate does not rewrite production data, it appends to it, from a test, forever. These rows are indistinguishable from real signups to build phase 4.6's counting and to build phase 4.13's history rail except by the `@example.invalid` suffix, which nothing enforces or filters on | open | Established by reading P3 end to end for a cleanup path and finding none, then confirming live that `POST /auth/signup` against the production API returns 201 and creates a durable account (id c1bae48f-277b-4f68-978a-2b0510568876, created during this review) | unassigned |
| F-4.15-A-17 | minor | `docs/build/Release_flow.md`, the document T-4.15-08 makes "the source of truth for how a change reaches production", states a false fact in the present tense and builds a required step on it. Line 37: "`GET /health` returns an `app_env` field ... anyone holding a URL can call its `/health` endpoint and read `app_env` to confirm which deployment answered". Both live APIs return exactly `{"status":"ok"}`. Line 49 then makes it step 7 of the release procedure: "Confirm the deployment by calling `GET https://search-agent-api-production.up.railway.app/health` and checking that `app_env` reads `production`". The first operator to follow the written release procedure will find the confirmation step impossible, and will be doing so immediately after a production merge, which is the worst moment to discover the runbook is describing code that is not deployed. Same root cause as F-4.15-A-09; filed separately because it is a different artifact with a different ticket and needs its own fix or its own caveat | open | Established by reading lines 18 to 52 of Release_flow.md against the measured live `/health` responses on both deployments | unassigned |
| F-4.15-J-01 | major | P1's docstring CLAIMS a check the code does not perform: "the service ids reported by the two deployments are compared too, because two hostnames can still front one service" (`tests/system_03_search_agent/tools/test_release_environments_premise.py:181-183`). The body of `test_p1_both_environments_answer_at_different_hostnames` (lines 185-208) compares only `prod["api"] != dev["api"]`, `prod["web"] != dev["web"]` and each `/health` status. No service id is read, and `/health` does not return one. So the second half of P1, the half the docstring calls "the half that matters", does not exist. Two distinct hostnames aliased onto one deployment pass P1 | open | Found by reading the docstring as a claim to be tested per `.claude/rules/self-eval-loop.md`, then diffing it against the body | judge |
| F-4.15-J-02 | major | The Goal contract in `tracker/phase_4.15.md:82-88` still states the PRE-REDESIGN outcome and three of its five done-when items are literally false against what shipped. Item 1 requires "Two Railway environments exist in `system3-search-agent`"; two PROJECTS shipped (F-4.15-03), and the fixture records `project_id` `0f85f78e...` and `4879a10f...`. Item 2 requires "`production` the environment watches `main`"; production watches `production`. Item 3 requires "`main` exists on origin"; T-4.15-12 deleted it. The same staleness runs through "What the refinement settled" (line 70, "A second environment in the existing project"), the Coverage bullet at line 153 ("merge-blocking on `main`"), and the arms table at lines 99-108, which lists 8 arms while the gate ships 9 (P9). The tickets table was rewritten for the redesign and the contract that grades it was not, so the phase's own stated outcome is unmet on its face | open | Found by grading the finished tickets against the goal contract's done-when items rather than against the ticket list | judge |
| F-4.15-J-03 | critical | The mutation harness does NOT cover P6 or P9, so two of the nine arms are unproven, and both the harness and the phase file assert otherwise. `test_release_environments_mutation.py:3-5` states "Every arm in `test_release_environments_premise.py` is asserted here to go RED when the property it guards is broken"; `grep -n "def test_" ` on that file returns six test functions and the parametrised set covers P1 (x3), P2, P3, P4 (x2), P5 (x2), P7 and P8 (x3), 14 cases with NO case for `test_p6_develop_is_not_missing_a_variable_production_has` and none for `test_p9_the_release_workflow_holds_the_no_shell_and_no_injection_rules`. This makes T-4.15-09's acceptance ("every arm proven red under its own mutation AND green under a healthy control") false as written, and goal-contract done-when item 5 ("every arm is proven able to fail by a mutation harness") unmet. The harness's own coverage statement admits only a narrower gap, "it does not mutate P6's `railway` subprocess boundary BEYOND ITS PARSE", which reads as partial coverage where there is none | open | Found by enumerating the parametrise lists and the module-level test functions against the nine arms, per the brief's mutation checklist | judge |
| F-4.15-J-04 | critical | P3, the arm the phase file calls "the arm that carries the most weight", CANNOT detect a shared `AUTH_SECRET` on the real deployments, and the mutation harness's fake transport is what hides it. On the real system `GET /auth/me` runs `resolve_user_from_bearer_token`, which decodes the token AND THEN looks the subject up in that deployment's own database (`src/system_03_search_agent/auth/dependencies.py:109-111`, `select(User).where(User.id == user_id)`), returning the same 401 when the row is absent. The two deployments have separate Postgres instances, so a token minted on develop is rejected by production on the MISSING USER ROW whether or not the signing key is shared. P3 therefore measures database separation, which is already P2's property, and asserts `AUTH_SECRET` separation, which is a CORRELATE of it. That is the safety-by-proxy shape the premise gate's own docstring (lines 19-27) says it was written to avoid. The mutation harness misses it because `_World.get`'s `/auth/me` branch (`test_release_environments_mutation.py:140-148`) returns `200 if minted_by == env or self.shared_auth_secret` and NEVER consults `self.accounts` or `_owns`, so the fake transport models a token check with no database lookup at all. MEASURED: re-running P3 unchanged against a subclass of `_World` whose `/auth/me` also requires the user row to exist in the environment being asked returns GREEN under `shared_auth_secret=True` (arm did not detect the shared secret) and GREEN under the healthy control, while the shipped `_World` returns the expected red. The fake transport is unfaithful in exactly the direction that makes the arm look sound | open | Found by reading the real `/auth/me` code path the arm depends on, then re-running the arm against a transport corrected to match it | judge |
| F-4.15-J-05 | minor | `get_health`'s docstring in `src/system_03_search_agent/adapters/web_sse/app.py` cites the wrong finding: "which is finding F-4.15-02: a variable can be configured, carried by an environment duplicate, and load-bearing in nobody's code at all". F-4.15-02 in this file is the `railway service source connect` scoping defect; the described property matches nothing in the Findings table. A reader chasing the reference lands on an unrelated finding | open | Found by following the finding reference in the docstring back to the Findings table | judge |
| F-4.15-J-06 | major | P7 does not assert the property the goal contract and the gate's own arms table give it, and the docstring names a live equivalent that does not exist. The contract (line 107) and the premise docstring both state P7's property as "`env.example` names every non-Railway variable the API service is given". What `test_p7_env_example_documents_every_variable_the_deployment_sets` actually asserts is that `env.example` names every variable in `deployment_sets`, a HARDCODED 26-name literal snapshotted on 2026-08-27 (premise file, the `deployment_sets = {...}` block). Add a 27th variable to the Railway service tomorrow and leave it out of `env.example` and P7 stays green: F-4.15-01 recurs undetected. The docstring's mitigation, "the live equivalent is P6", is false and MEASURED so: `inspect.getsource(test_p6_...)` contains no reference to `env.example`, `_ENV_EXAMPLE` or `_documented_variable_names`; P6 compares production's variable names against develop's and never against the documented set. So no arm in this gate compares the live deployment to `env.example`, and the phase's Coverage section does not name the gap | open | Found by reading P7's assertion against the property the contract assigns it, then checking P6's source for the claimed live equivalent | judge |
| F-4.15-J-07 | major | `write_changelog.sh` glues the first version heading straight onto the preamble prose, and the code comment above the offending block claims the opposite. Lines 84-87 state: "Split at the FIRST `## ` heading rather than at a fixed line number. The first version of this counted `head -n 6`, which cut the preamble one line short of its trailing blank and produced a version heading glued to the prose. A structural split cannot drift when the preamble is reworded." MEASURED by running the shipped script against the shipped `CHANGELOG.md` in a scratch repository: the output is `...writes its section directly below this line.` on line 9 and `## v0.1.0 (2026-08-28)` on line 10, with no blank line between them. The cause is that the checked-in `CHANGELOG.md` has no trailing blank line, and the structural split preserves `head.md` verbatim; it never emits a separator. The defect persists on every later release, since `split_at` then lands on the existing `## v0.1.0` and lines 1-9 are copied unchanged again. The comment asserts a correctness property no test asserts | open | Found by executing the script against the real `CHANGELOG.md`, per "do not accept verified locally as evidence" | judge |
| F-4.15-J-08 | major | The shipped `CHANGELOG.md` preamble is not the preamble `write_changelog.sh` bootstraps, and the checked-in one carries a sentence that the first release makes permanently false. `CHANGELOG.md` lines 8-9 read "No release has been cut yet. The first push to `production` creates `v0.1.0` and writes its section directly below this line." The script only writes its own preamble when the file is ABSENT (lines 72-82), so the checked-in text survives every release, and the measured output above renders that sentence immediately above the v0.1.0 section it denies exists. `docs/build/Release_flow.md:65` carries the same sentence and has the same shelf life. Nothing removes either | open | Found in the same scratch-repository run that produced F-4.15-J-07 | judge |
| F-4.15-J-09 | minor | `write_changelog.sh` silently truncates any commit subject containing a `|`. The log format is `%s|%h` (line 51) and the split is `subject="${line%%|*}"`, which cuts at the FIRST pipe rather than the last. MEASURED: a commit `chore: a subject with a | pipe in it` renders in the generated changelog as `- a subject with a  (1cddf63)`, dropping "pipe in it" with no warning. `%x00` or a `%%|` last-field split would avoid it | open | Found by including a pipe-bearing subject in the scratch-repository run | judge |
| F-4.15-J-10 | major | The back-merge pull request the release workflow opens into `develop` will run NO CI, and neither the workflow, the scripts, nor `docs/build/Release_flow.md` says so. `open_backmerge_pr.sh` runs `gh pr create` with `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` (`release.yml:77`). GitHub's recursive-workflow prevention means events raised by `GITHUB_TOKEN`, a `pull_request` opened by it included, do not start workflow runs, so `ci.yml`'s unfiltered `pull_request` trigger never fires for this pull request. Combined with F-4.14-A-04 (CI advisory, no branch protection), the phase adds an automated, permanently uncheckable path into the default branch: a back-merge carrying a changelog commit merges into `develop` with zero gates and zero checks displayed. `Release_flow.md`'s "What this flow does not protect against" section lists five gaps and not this one, and step 8 tells the reader to "Review and merge" as though checks would be present | open | Found by tracing which credential opens the pull request and what that implies for the `pull_request` trigger | judge |
| F-4.15-J-11 | minor | Both release scripts read `git log` through a process substitution (`derive_version.sh:55,66`, `write_changelog.sh:51`), where `set -euo pipefail` does not apply. A `git log` that fails there produces an EMPTY stream rather than an abort, so `derive_version.sh` falls through with `bump` still at its `patch` default and publishes a permanent tag anyway, and `write_changelog.sh` writes a section with no entries. This is the one path in the release line where a silent failure still results in a tag rather than an aborted run, which is the trade-off `derive_version.sh:36-39` explicitly says it does not want | open | Found by checking `set -e` and `pipefail` scope against each command substitution and process substitution in the four scripts | judge |
| F-4.15-J-12 | minor | The comment this phase added to `.github/workflows/ci.yml:44-48` drops a word and inverts its own meaning: "Build phase 4.15 measured that this is what already makes a release pull request into `production` run every gate, with change needed." It should read "with NO change needed", which is what the measurement in `tracker/phase_4.15.md:47` and `Release_flow.md:46` both say. As written it tells the next reader that a change IS needed to the trigger the comment exists to tell them not to touch | open | Found by reading the ci.yml diff against the measurement it cites | judge |
| F-4.15-J-13 | minor | `tag_and_release.sh:7-10` claims a correctness property nothing tests: "without it this workflow pushes to `production`, that push triggers this workflow, and the second run tags a version on top of the first. The marker is what makes the loop terminate." Two problems. First, no arm asserts the `[skip ci]` marker is present, so deleting it from line 22 leaves the whole gate green. Second, the marker is not in fact what makes the loop terminate: the push and the pull request both use `secrets.GITHUB_TOKEN`, and GitHub does not start workflow runs from events raised by that token, so loop termination holds even with the marker removed. The comment names the weaker of the two mechanisms as the load-bearing one, and per `.claude/rules/self-eval-loop.md` a comment asserting a correctness property must have a test asserting the same property | open | Found by tracing which of the two loop-prevention mechanisms is actually load-bearing, then searching the gate for any assertion on the marker | judge |
| F-4.15-06 | minor | P3a's first version tampered with the LAST character of the token signature and both deployments returned 200, which reads exactly like "the API accepts forged tokens" and would have been filed as a critical. It is not. A base64url character carries 6 bits and an HMAC-SHA256 signature is 32 bytes, so the final character holds 2 significant bits and 4 that decode to nothing: a different string can decode to identical signature bytes, which the server then correctly accepts. Tampering mid-signature makes both arms pass. RECORDED rather than silently corrected because the near-miss is the lesson: a faulty probe produces a false CRITICAL just as easily as a false pass, and the tell was that a forged-token acceptance bug cannot explain a token that still verifies while a no-op edit can | fixed | Found by disbelieving the arm's own result and asking what else could produce it, before filing anything | lead |

## History

- 2026-08-27, lead: phase opened. Refinement settled the same day and logged in `DECISIONS.md`; board row moved from `product_refine` to `refined`.
- 2026-08-27, lead: measured the Railway topology, the branch list, the CI triggers and the variable sets before writing any ticket. Two measurements changed the plan, recorded above. F-4.15-01 filed from the `env.example` diff.
