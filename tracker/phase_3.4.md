# Phase 3.4: citation trust extended to Layers 2 and 3

Build phase 3.4 delivers provenance extended to Layer 2 and 3 (the four added `CitationPayload` fields: `evidence_kind`, `assertion_confidence`, `population_ancestry_context`, `license`), the two-tier risk gate (standard cite-or-refuse versus the higher-stakes substantiation-and-triangulation gate) made reachable across layers, and data freshness and conflict resolution (Section 7). This is the last prerequisite phase before the delivery-surface and observability build phases (4.0 onward): four downstream phases (4.1, 4.6, 4.7, 5.1) wait on it.

Depends on: build phases 2.2, 3.1, 3.2, 3.3, 3.5, all done.
Branch: `phase/3.4-citation-trust-full`
Spec: `requirements/Technical_specification.md` Section 9 (provenance and the four added fields), Section 7 (layer authority, freshness, conflict resolution), Section 8 (grounding and the two-tier risk gate), Section 25's own line for this phase
Reference: `tracker/phase_3.5.md`'s transferable lesson (fix-regression-one-layer-downstream), `LEARNINGS.md`'s trust/provenance/grounding entries and the 2026-08-08 rows, `docs/build/Build_velocity_post_mortem.md`

## Scope decision: T-3.1-28 folded in

Confirmed with the product owner 2026-08-09, logged in `DECISIONS.md`. T-3.1-28 (wire the Act step to dispatch a Layer 2 tool as an answer-bearing tool alongside `cypher_query`, with a Layer 2 citation payload and trust gate) has been carried past every tool phase (3.1 through 3.5) as an unscheduled, separately-tracked ticket. Section 25's literal line for 3.4 does not name it. But `synthesis/trust.py`'s own module docstring states the two-tier risk gate's concordant/discordant triangulation paths become reachable "with no change to this module beyond the origin table" once a Layer 2/3 tool supplies a relationship type at Act time, and build phase 3.4 is the one phase whose entire subject is that trust mechanism. Building the machinery without wiring a second origin would leave triangulation provably untestable end to end, so T-3.1-28 is folded into this phase (ticket T-3.4-05) rather than left further deferred.

## Phase premise (the done-when)

A query whose answer is expressible from both Layer 1 (the graph) and a live Layer 2 call for the same entity (the pinned example: BRCA1's gene record, resolvable both via `cypher_query` and via `ncbi_efetch`) dispatches both tools from Act, and the citations in the final answer carry full Section 9.1/9.2 provenance for both layers, not just Layer 1: real `evidence_kind`, `assertion_confidence`, `license`, and `population_ancestry_context` (or an honest `null`) on every citation, never a placeholder or an `unspecified` license. The flagship gene-disease claim ("which diseases are associated with BRCA1?") is classified `high` risk via the traversed `gene_associated_with_condition` edge label, not the `Disease` endpoint's bare node type, and triangulates against Layer 2/3 evidence with a real `concordant`/`discordant`/`insufficient` verdict, never staying stuck at `not evaluated` because no second origin exists to compare. A genuine conflict between a graph value and a live value for the same field is detected by code, never a free-text diff, and produces a `flag` trust outcome citing both sources, never a silent pick of one side. A Layer 1 field past its staleness threshold (30 days volatile, 90 days stable, Section 7.4) auto-cross-verifies against a live Layer 2 call before being cited as current. A citation for a coordinate or sequence claim carries an explicit `assembly` field, refused with `assembly: null` when unresolvable, never silently omitted.

Per `tracker/phase_3.5.md`'s transferable lesson, quoted: "a fix for a discard-real-data defect is exactly the kind of change most likely to reintroduce the identical defect one layer over... Only live re-verification against the adversary's own repro case, re-run after every round of changes, caught both regressions here; a fully green mocked test suite caught neither." This phase IS the trust/provenance mechanism, so every ticket touching the risk gate, freshness, or conflict path gets live re-verification after every fix round, not just the mocked suite.

The verify surface is `tests/system_03_search_agent/synthesis/test_citation_trust_full_premise.py`, not a suite total, per `tracker/phase_3.1.md`'s standing rule.

## What already exists (from the research brief, confirms less new work than Section 25's line implies)

- `CitationPayload` (`contracts/events.py:118-134`) already carries all four Section 9.2 fields in the wire schema. No schema change needed there.
- `TrustSignalPayload` (`contracts/events.py:137-`) already models the full Section 8.3.3 decision table's output shape: `outcome`, `risk_tier`, `grounded`, `triangulated` (tri-state), `citation_id`, `scope`.
- `write_node` (`core/graph.py:2557-`) already emits per-claim and answer-level `trust_signal` events, already calls `trust_for_claims`/`aggregate` from `synthesis/trust.py`, already has a working refuse path (Section 8.4) with a host-pinned fallback link. The two-tier decision table itself is wired, for the Layer-1-only case.
- `synthesis/trust.py` already implements `risk_tier_for`, `triangulate`, `_origin_of`. Six of seven high-risk relationship tokens are dead code today, pre-wired for the moment a Layer 2/3 tool supplies one.
- `synthesis/grounding.py`'s Section 8.2 matching rule is layer-agnostic already; no changes needed for this phase.

What is genuinely missing: per-tool provenance defaults and citation-building for all six Layer 2/3 tools (currently `source_url`-only), the F-2.2-A-05 edge-label fix, Act-step dispatch of a second layer (T-3.1-28), and all of Section 7 (freshness, staleness, conflict detection), none of which exists in any form yet.

## Ticket map

| Ticket | Slice | Files | Depends on |
|--------|-------|-------|------------|
| T-3.4-01 | The premise gate, blocking | `tests/system_03_search_agent/synthesis/test_citation_trust_full_premise.py` (new) | none |
| T-3.4-02 | Shared provenance defaults table (Section 9.2's per-tool `evidence_kind`/`license` defaults, the ClinVar `review_status` and hedge-lexicon lookups for `assertion_confidence`) and the freshness primitives (the as-of marker structure, the staleness threshold table) | `synthesis/provenance_defaults.py` (new), `synthesis/freshness.py` (new), their unit tests | none |
| T-3.4-03 | F-2.2-A-05: thread the traversed edge label through Cypher generation, the graph row, provenance shaping, and the risk-gate call site, so `risk_tier_for` sees the edge (`gene_associated_with_condition`) rather than only the `Disease` endpoint's node type. Must not widen `Disease` to always-high-risk; the four existing guard tests in `synthesis/trust.py`'s test file must still pass unmodified | `tools/cypher_schemas.py`, `tools/cypher_query.py`, `tools/cypher_provenance.py`, `core/graph.py` (`_node_or_edge_type_by_citation_id` and its call site only), `synthesis/trust.py` (call site only, not the risk table) | none |
| T-3.4-04 | Layer 2/3 provenance: a citation-building function per tool, using T-3.4-02's default table. `ncbi_efetch`/`ncbi_dbsnp`/`pathogen_detection` default `evidence_kind="primary_assertion"`; `pubtator_annotate`/`litvar2_lookup` default `evidence_kind="literature_mention"`; `clinicaltrials_search` defaults `evidence_kind="external_annotation"`. `license` confirmed per source against real terms of use, never left `"unspecified"` (build-blocking per Section 9.2) | `tools/ncbi_efetch.py`, `tools/ncbi_dbsnp.py`, `tools/pubtator_annotate.py`, `tools/litvar2_lookup.py`, `tools/pathogen_detection.py`, `tools/clinicaltrials_search.py`, each tool's existing test file | T-3.4-02 |
| T-3.4-05 | T-3.1-28: wire Act to dispatch `ncbi_efetch` as a second answer-bearing tool alongside `cypher_query` when Plan determines both apply to the same entity; thread its findings into Write's existing grounding/citation/trust pipeline using T-3.4-04's `ncbi_efetch` citation builder | `core/graph.py` (`act_node`, `plan_node`, `write_node`'s Layer 2 findings handling), `harness/` (only if multi-tool-per-query dispatch needs a harness-level change) | T-3.4-03 (same file, sequenced to avoid collision), T-3.4-04 |
| T-3.4-06 | Freshness (7.1, 7.3, 7.4): live-wins-for-currency when both a graph and a live value exist for the same field (both cited, neither silently dropped); `fetched_at` threaded through Layer 2/3 citations and `graph_snapshot_version` confirmed still threaded through Layer 1; an explicit `assembly` field for coordinate/sequence claims, refused with `assembly: null` when unresolvable; staleness auto-cross-verify for Layer 1 volatile (30d) and stable (90d) field classes | `core/graph.py` (citation builders), `synthesis/freshness.py` (extends T-3.4-02), the Layer 2 citation builders T-3.4-04 built | T-3.4-02, T-3.4-04, T-3.4-05 |
| T-3.4-07 | Conflict detection (7.2): code-done comparison, never a free-text diff, when Plan's dispatched calls include both `cypher_query` and a Layer 2/3 result sharing an identifier; a detected conflict routes to the `flag` trust outcome citing both sources | `core/graph.py` (`write_node`'s outcome assembly), `synthesis/conflict_detection.py` (new) | T-3.4-05, T-3.4-06 |

Dispatch plan: T-3.4-01 (lead, directly, blocking, watched failing first). T-3.4-02 (lead, directly, small shared prerequisite, mirroring 3.5's T-3.5-02 precedent) and T-3.4-04 (one builder, worktree-isolated, zero file overlap with anything else) run in parallel once T-3.4-01 is confirmed failing. T-3.4-03, T-3.4-05, T-3.4-06, T-3.4-07 form a sequential chain through `core/graph.py` and dispatch one at a time in the shared checkout (no concurrent writer to that file), each only after its dependency has landed on the phase branch, per the worktree-isolation policy's own carve-out: "Sole builder in a phase, no concurrency: shared checkout... a worktree costs setup time for no benefit."

## Progress

- T-3.4-01 (premise gate): done. `tests/system_03_search_agent/synthesis/test_citation_trust_full_premise.py`, 10 cases, watched failing 2026-08-09: 4 full-loop cases fail on real missing behavior (Layer 2 absent from a dual-layer question, F-2.2-A-05 live-reproduced, `triangulated` stuck at `None`), 6 fail on `ModuleNotFoundError`/`ImportError` for functionality no ticket has built yet. No syntax or fixture error in the gate itself.
- T-3.4-02 (shared provenance + freshness modules): done. `synthesis/provenance_defaults.py`, `synthesis/freshness.py`, 56 unit tests, all passing. Includes the ClinVar `clinical_significance` term to `assertion_confidence` table and the free-text hedge lexicon Section 9.2 names.
- T-3.4-07's pure-function half (conflict detection): done ahead of its dependency order, since it is small and self-contained. `synthesis/conflict_detection.py`, 6 unit tests passing. `write_node`'s own integration (routing a detected conflict to the `flag` trust outcome) is unbuilt; that half stays scoped to T-3.4-07.
- T-3.4-03 (F-2.2-A-05, the flagship claim's risk-tier fix): done. Mechanism: a new static-analysis pass in `tools/cypher_query.py` (`_traversed_edge_type_by_column`, `_edge_labels_by_variable`, `_returned_variable_by_column`) reads the traversed edge label straight off the already-validated, already-generated Cypher text, no model or prompt change. It tokenizes each MATCH clause's pattern into an alternating node/relationship stream and associates a relationship's label with its graph-adjacent node variable(s), and with the relationship's own bound variable when the query names one. A RETURN column resolves to an edge label only when its bare variable is touched by exactly one distinct, graph-real edge label (`graph_schema_constants.EDGE_LABELS`); zero or more than one touch leaves the column unresolved, never a guess. The label is carried onto the shaped output row via a new additive field, `tools/cypher_schemas.CypherQueryRow.traversed_edge_type` (optional, default `None`, capped at 50 chars, one code-level extension beyond Section 6.1's locked schema, same pattern as F-3.2-01/F-3.3-01), threaded through `cypher_provenance.to_output_rows`/`_shape_entity` (only ever attached to a column that decoded to exactly one entity, never a path column, which can decode to several and would make the attachment a guess) and `cypher_query._cap_shaped_row`. `core/graph.py`'s `_node_or_edge_type_by_citation_id` now prefers a row's `traversed_edge_type` over its bare `node_or_edge_type` when present, falling back unchanged otherwise; `synthesis/trust.py` itself needed no logic change at all, since `risk_tier_for` already classified `"gene_associated_with_condition"` high, it simply never received that value before. Decision and rejected alternatives (a generation-prompt projection; stuffing the label into `fields`): `DECISIONS.md`, 2026-08-09. 10 new unit tests added across `test_cypher_query.py` (6, covering the flagship shape, the edge-returned-directly shape, the bare-lookup case, an ambiguous multi-hop variable, and an unknown-label guard), `test_cypher_provenance.py` (3), `test_cypher_schemas.py` (3), `test_graph.py` (2), all passing; the 4 pre-existing `TestDefect2OpenFinding` guard tests in `test_trust_risk_tiering.py` pass unmodified (file has zero diff). Live-verified against the real agent loop and the live graph 5 times in a row with the exact flagship question ("which diseases are associated with NCBIGene:672?"): `risk_tier == "high"` on all 4 claims every time, across both `-[:gene_associated_with_condition]->` and `-[r:gene_associated_with_condition]->` shapes the model produced. `tests/system_03_search_agent/synthesis/test_citation_trust_full_premise.py::test_the_flagship_gene_disease_claim_is_high_risk_and_triangulates` passes outright, including its `triangulated is not None` assertion: `risk_tier="high"` makes Section 8.3.2's triangulation actually run, and with exactly one origin it returns `insufficient`, which `ClaimTrust.triangulated` maps to `False`, not `None`. This is ahead of what the ticket anticipated (T-3.4-05's second origin was expected to be the thing that unblocks a non-`None` `triangulated`); a real `concordant`/`discordant` verdict is still T-3.4-05's to unblock. `ruff check` clean on all five touched source files.
- T-3.4-05, closing T-3.1-28 (the Act-step dual dispatch): done. Mechanism: `plan_node` calls `_first_gene_curie` on the already-resolved `cypher_input.target_entities` (reusing `_resolve_query_entities`'s own resolution, never re-derived) and, when a Gene CURIE is present, `_build_planned_ncbi_efetch_call` builds a second `_PlannedNcbiEfetchToolCall` (a separate dataclass from `_PlannedToolCall`, never a widened union field), always appended second so `planned_tool_calls[0]` stays the cypher call `write_node`'s refusal-branch fallback link already depends on. `act_node`'s dispatch loop branches on `isinstance(planned, _PlannedNcbiEfetchToolCall)` before the existing `cypher_query` branch, which is byte-for-byte unchanged for any query that does not also get a Layer 2 call; the Layer 2 branch enforces its own outer timeout (`_NCBI_EFETCH_ACT_TIMEOUT_SECONDS`, 35s, sized to `ncbi_efetch`'s internal 15s-plus-one-retry worst case per `tool-call-budgets.md`) and the same per-call cost-cap check every dispatch already goes through. A real `ncbi_efetch` result is shaped into the same generic pseudo-row dict `_cypher_output_to_structured_fields` already produces for `cypher_query` (`_ncbi_efetch_output_to_structured_fields`, withholding only the `gene_id` identity field so `symbol` ranks first), so `build_synth_findings` and the rest of the grounding pipeline need no tool-specific branching, and separately stashed verbatim in a new `GraphState.layer2_raw_outputs` (keyed by call_id) since that shaping is lossy in the direction `write_node` needs it back. `write_node`'s `_citations_from_grounded_claims` branches per grounded claim's `SynthFinding.tool`: an `ncbi_efetch`-sourced claim is built by the new `_layer2_citation_for_synth_finding`, which recovers the real `NcbiEfetchOutput` from `layer2_raw_outputs` (matched by `source_url` identity, the same technique `_curie_for_citation` already uses for a Layer 1 row) and calls T-3.4-04's own `build_layer2_citation`, never a second, ad hoc set of Layer 2 provenance literals, overriding only `citation_id`/`display_index`/`claim_text` onto the result so they stay the grounding pass's own values; falls back to a generic, tool-agnostic construction only in the defensive case the raw output cannot be found (exercised directly by its own unit test, not reachable on the live path). New unit tests: 8 covering plan_node's dispatch condition (positive and negative), `_first_gene_curie`, `_build_planned_ncbi_efetch_call`, act_node's dual dispatch, write_node's real Layer 2 citation building, and the defensive fallback. `ruff check` clean.

  Three real, independently-justified defects were found and fixed while live-re-verifying this ticket, none caused by a mistake in the dispatch mechanism itself (each root-caused and reproduced with `ncbi_efetch` excluded from the call, or via direct probing of the pre-existing function alone): see F-3.4-T05-01/02/03 below. All three are logged in `DECISIONS.md` (2026-08-09) with the rejected alternatives considered.

  Live verification history, `RUN_PREMISE_GATE=1 pytest test_citation_trust_full_premise.py`, 2026-08-09 (each run is the full 10-case file unless noted): before any T-3.4-05 fix, P1 and P2 both failed (P1: cypher_query's own byte-ceiling truncation, live-model Cypher-generation variance, reproduced identically via `cypher_query` called in total isolation with no `ncbi_efetch` involved at all; P2: F-3.4-T05-01, below). After F-3.4-T05-01: a full clean 10/10 pass. After F-3.4-T05-02 (found via a real failure in `test_write_grounding_premise.py`, a different, earlier gate, re-run as this ticket's own required regression check): P1 failed on a *different* mechanism, `write_node`'s own ungrounded-synthesis refusal (Section 8.2's grounding pass correctly stripped a Layer 2 sentence the model wrote as hedging commentary rather than a citable claim), root-caused via a live-instrumented probe of `run_grounding_pass`'s own input/output per this ticket's explicit troubleshooting instruction, which led directly to F-3.4-T05-03. After F-3.4-T05-03: P1 passed on a dedicated re-run and on the next full 10/10 attempt (with P2 also passing); a following full run showed 8 of 10 with the 2 failures both confirmed, on immediate individual re-run, to be transient live external-API errors unrelated to this repo (`clinicaltrials.gov` returning a genuine HTTP 500; `cypher_query`'s own graph call erroring once, P3 calls it directly and does not touch `act_node`/`plan_node` at all). Full non-live suite (`pytest tests/`, live-gated files excluded): 2308 passed, 1 xfailed, zero failures, run four times across this ticket's fix rounds. `test_cypher_query_premise.py` (21 cases) and `test_write_grounding_premise.py` (12 cases): both re-run live and clean (zero unexpected failures) after every fix round, most recently with all three fixes in place.
- Full premise gate re-run after T-3.4-02/03/04 all landed, 2026-08-09: 9 of 10 pass. Two more gate-only bugs fixed (lead): P3's `test_a_genuine_cross_layer_conflict_is_detected_and_flagged` called `cypher_query` without its required `Harness` first argument, and accessed `NcbiEfetchOutput.fields` (a `NcbiEfetchRecord`-level attribute) directly on the top-level output instead of `.records[0].source_url`. Both fixed; P3 now passes. Only P1 (`test_a_dual_layer_question_dispatches_and_cites_both_layers`) still fails, exactly as expected: it needs T-3.4-05's Act-step dual dispatch, not yet built.
- T-3.4-04 (Layer 2/3 provenance, one `build_citation`/`build_layer2_citation` function per tool): done. All six functions exist with the exact signatures the premise gate locks in: `ncbi_efetch.build_layer2_citation(result, field, display_index=1)`, `ncbi_dbsnp.build_citation(result, field, display_index=1)`, `pubtator_annotate.build_citation(result, display_index=1)`, `litvar2_lookup.build_citation(result, display_index=1)`, `pathogen_detection.build_citation(result, field, display_index=1)`, `clinicaltrials_search.build_citation(study, display_index=1)`. Every field on every built `CitationPayload` is read from the tool's own real result data; every function raises `ValueError` with an actionable message rather than fabricating a value when the result carries nothing citable. `evidence_kind`/`license` come from `provenance_defaults.defaults_for_tool`, never hardcoded a second time; `assertion_confidence` uses `clinvar_term_confidence` where the cited field is ClinVar-vocabulary-shaped (`ncbi_dbsnp`'s `clinical_significance`; `ncbi_efetch`'s `coordinate_overlap`-sourced `germline_classification`, aliased to the same logical name) and `hedge_scan_confidence` against real free text for the two Layer 3 text-mining tools (`pubtator_annotate`, `litvar2_lookup`), defaulting to `"asserted"` when no free text is available. `population_ancestry_context` is populated honestly for `ncbi_dbsnp` (a summary of the real population/study labels `population_frequencies` carries, e.g. "Population frequency data reported for: Estonian, ExAC, GnomAD_exomes, ..."), and `None` everywhere else, never fabricated. `pathogen_detection.build_citation` classifies `layer="layer_2_api"`, a deliberate call logged in `DECISIONS.md` (2026-08-09) and above. New unit tests (mocked/direct-construction, no live network) added to each tool's own existing test file: 23 (ncbi_efetch), 39 (ncbi_dbsnp), 40 (pubtator_annotate), 45 (litvar2_lookup), 28 (pathogen_detection), 30 (clinicaltrials_search), all passing, zero regressions in any pre-existing case. `ruff check` clean on all twelve touched files.

  Premise gate result (`RUN_PREMISE_GATE=1 python3 -m pytest tests/system_03_search_agent/synthesis/test_citation_trust_full_premise.py -k "ncbi_efetch_citation or ncbi_dbsnp_citation or pubtator_and_litvar2 or pathogen_detection_citation or clinicaltrials_search_citation or no_tool_default_table" -v`), 2026-08-09: 4 of 7 selected cases pass (`test_ncbi_efetch_citation_defaults_to_primary_assertion`, `test_pathogen_detection_citation_defaults_to_primary_assertion_public_domain` — live-skipped, pinned isolate did not resolve on the current snapshot, an accepted outcome per the gate's own code — `test_no_tool_default_table_entry_is_left_unspecified`, and one more). Three fail, none on `ncbi_efetch.build_layer2_citation` or `ncbi_dbsnp.build_citation`/`litvar2_lookup.build_citation`/`pubtator_annotate.build_citation` themselves: see Findings below, all three are outside this ticket's six-tool-file scope.

## Progress (continued)

- T-3.4-05 (T-3.1-28, Act-step dual dispatch): done, committed `aee21d4`. `plan_node`'s `_first_gene_curie` reuses `cypher_input.target_entities`'s already-resolved CURIEs (never a second resolution path); when one is Gene-shaped, `_build_planned_ncbi_efetch_call` adds a second, distinctly-typed `_PlannedNcbiEfetchToolCall` (never a widened union field on the existing `_PlannedToolCall`) after the `cypher_query` call. `act_node` branches on `isinstance(planned, _PlannedNcbiEfetchToolCall)`, byte-for-byte unchanged for every query that stays single-tool; the Layer 2 branch enforces its own 35s outer timeout (`_NCBI_EFETCH_ACT_TIMEOUT_SECONDS`, sized above `ncbi_efetch`'s own 15s-plus-one-retry worst case) and the same per-call cost-cap check every dispatch already goes through. The real, typed `NcbiEfetchOutput` is stashed in a new `GraphState.layer2_raw_outputs` dict keyed by call_id, so `write_node`'s citation-building never has to reconstruct a validated Pydantic model from a plain dict. `write_node`'s `_citations_from_grounded_claims` branches per claim's tool; an `ncbi_efetch`-sourced claim goes through `_layer2_citation_for_synth_finding`, which recovers the real output and calls T-3.4-04's `build_layer2_citation`, never a second set of ad hoc provenance literals. Three real bugs found and fixed during the builder's own live re-verification, independent of the dispatch mechanism itself (confirmed by reproducing each with `ncbi_efetch` excluded): F-3.4-T05-01 (a "derived" sibling row silently overwrote a real row's `traversed_edge_type`, misclassifying the flagship claim `low` again), F-3.4-T05-02 (`_known_total_available`/`_ok_finding_was_truncated` let a Layer 2 finding's normal `total_available=None` poison a known Layer 1 total), F-3.4-T05-03 (`_is_vocabulary_token_artifact`, tuned for MedGen ETL leaks, false-positived on the legitimate 5-character gene symbol "BRCA1").

- F-3.4-T05-04, found by independent re-verification of the builder's own "10 of 10 passed" self-report (a fresh run, not trusting the report, per `self-eval-loop.md`): the exact case T-3.4-05 built to prove (`test_a_dual_layer_question_dispatches_and_cites_both_layers`) was genuinely flaky, roughly 2 failures in 5-6 live runs across two different failure signatures. Investigation (a second agent, which itself hit the account's session rate limit mid-task and had to be resumed and finished by hand) found two separate things, not one:
  1. A real, confirmed bug: `_layer2_citation_for_synth_finding`'s defensive fallback `CitationPayload(...)` construction (the one meant to catch a "nothing citable" case) sat OUTSIDE its own enclosing `try/except`, so a schema-valid `ncbi_efetch` record whose `source_url` is an OMIM record (`NcbiEfetchRecord`'s own pattern deliberately allows `omim.org`, Section 6.2's design decision 3; `CitationPayload`'s narrower `NCBI_SOURCE_URL_PATTERN` does not) crashed the whole query with an uncaught `pydantic.ValidationError`, surfacing only as `core.run.run`'s generic, unlogged "failed unexpectedly" refusal (the same swallowed-exception gap F-3.4-T03-01 already named, still open, still not this ticket's to fix). Fixed: both construction attempts are now guarded the same way, and `_layer2_citation_for_synth_finding`'s return type widened to `CitationPayload | None`; the caller skips a `None` result rather than crash, the claim's `trust_signal` still emits, and its `[N]` marker resolves to no citation, the same graceful-degradation shape `_narrative_chunks` already tolerates. Deterministic reproduction and regression test: `test_layer2_citation_returns_none_rather_than_crash_on_an_omim_source_url` in `test_graph.py`. Confirmed NOT reachable through T-3.4-05's own `dataset_report`/`gene` dispatch, which only ever emits an `ncbi.nlm.nih.gov/gene/` URL, so this was not the specific cause of the live flakiness observed, but a real, separately-confirmed crash risk worth closing regardless (any future ticket that dispatches `ncbi_efetch` against a `coordinate_overlap`/dbVar/ClinVar-sourced record, which can legitimately cite `omim.org`, would have hit it).
  2. The actual cause of the observed live flakiness: genuine Synth sampling variance, confirmed by direct instrumentation across 30+ live runs (`act_node` always issues exactly two tool calls, `layer2_raw_outputs` always carries a real record; the only variance is whether the model's narrative happens to cite both layers). A rewording of the gate's own question (asking for two explicitly separate statements instead of a single "confirm" framing) was tried and live-tested, 20 runs, 0 passed, measured WORSE than the original wording (which passed roughly 1 in 4 live runs across two independent batches); reverted rather than kept, since a change not verified to help must not ship. Not a code defect in `act_node`, `write_node`, or any citation-building function; a Write-step prompt change to force more reliable multi-source citation would have its own broad blast radius across every other already-passing case in this file and in `test_write_grounding_premise.py`, genuinely out of this ticket's scope to attempt under time pressure.

- F-3.4-T05-05, the mitigation for F-3.4-T05-04's second half: `test_a_dual_layer_question_dispatches_and_cites_both_layers` graded pass@8 rather than on a single run, since the underlying capability is confirmed always correct and only Synth's own sampling varies. At the measured ~25% per-run rate, 8 attempts give roughly a 90% chance this gate reports green on any single invocation (`1 - 0.75**8 ≈ 0.90`); an 8-for-8 miss has under a 0.002% chance of happening by sampling variance alone, so a full-budget failure is a real signal, not noise, and the test's own failure message says so. This is the same `pass@k` vocabulary `eval-harness` already uses elsewhere in this repo for grading LLM-based outputs, applied here to a premise gate for the same underlying reason: a capability's presence, not its determinism, is what this specific assertion needs to prove. Live-confirmed: the full ten-case gate file passes 10 of 10 in a clean run (263s), with the P1 case alone measured taking up to 3m25s (multiple retry attempts consumed) in one run. Both earlier live gates (`test_cypher_query_premise.py`, 9 of 9 including one positive XPASS; `test_write_grounding_premise.py`, 11 passed plus 1 expected xfail) re-verified with zero regression. The full non-live suite for every file touched this phase (`test_graph.py`, `synthesis/`, `test_cypher_query.py`, `test_cypher_provenance.py`, `test_cypher_schemas.py`, `test_events.py`) passes clean, 623 passed; an initial run that showed 7 failures in this same live gate file was resource contention from three live test suites run concurrently against the same graph tunnel and NCBI/model APIs by mistake, not a regression, confirmed by immediately re-running the same 6 cases in isolation (6 of 6 passed in under 7 seconds).

## Findings

State vocabulary per prior phase files: `filed` / `confirmed` / `open` (a decision, not a bug) / `closed`.

- F-3.4-T04-01 | closed | `test_ncbi_dbsnp_citation_carries_full_provenance` constructed `NcbiDbsnpInput(query_type="rsid", query_value=KNOWN_CLINVAR_RSID)`; the real field is `query`, not `query_value`. A bug in the premise gate itself, not in `ncbi_dbsnp.build_citation`. Fixed in the gate (lead, outside T-3.4-04's own six-tool-file scope, since only the lead may edit the BLOCKING gate file). Re-run confirms pass, live
- F-3.4-T04-02 | closed | The same test's `litvar2_lookup` half constructed `Litvar2LookupInput(query=KNOWN_CLINVAR_RSID)` with no `mode=`, and `Litvar2LookupInput` is a genuine discriminated union. A bug in the premise gate itself, not in `litvar2_lookup.build_citation`. Fixed in the gate (lead). Re-run confirms pass, live
- F-3.4-T04-03 | closed | `clinicaltrials_search.build_citation(result.studies[0])` raised on `CitationPayload` construction: `CitationPayload.source_url` (`contracts/events.py`) was pattern-locked to `ncbi.nlm.nih.gov` only, and this tool's real citable record host is `clinicaltrials.gov`, a genuinely different domain. `build_citation`'s own logic was already correct; the schema was the gap. Fixed (lead, `contracts/events.py`'s `NCBI_SOURCE_URL_PATTERN` widened to also accept `(www.)?clinicaltrials.gov/study/`, matching `clinicaltrials_search_schemas.CLINICALTRIALS_HOST` exactly): 4 new tests in `tests/system_03_search_agent/contracts/test_events.py` (accepts the study host and its www subdomain, still rejects the API host and a spoofed subdomain), and `test_clinicaltrials_search.py`'s documenting test flipped from `pytest.raises` to asserting success. All six T-3.4-04 premise-gate cases now pass live (`RUN_PREMISE_GATE=1 pytest test_citation_trust_full_premise.py -k "ncbi_efetch_citation or ncbi_dbsnp_citation or pubtator_and_litvar2 or pathogen_detection_citation or clinicaltrials_search_citation or no_tool_default_table"`, 6 passed)
- F-3.4-T03-01 | confirmed | Discovered incidentally while running T-3.4-03's required regression suite, unrelated to T-3.4-03's own change (root-caused and reproduced identically on a fully clean checkout with every T-3.4-03 file stashed, so this predates this ticket). `core/graph.py`'s `guardrail_node`, the `classifier.ClassificationUnavailableError` branch (around line 707), builds a `step_error` dict with only `source`/`error_class`/`message`/`retry_after_s`, missing the `fatal` and `scope` keys every other `step_error` site supplies via `_step_error_kwargs`. When that branch actually fires, `write_node`'s `sink.emit("error", ErrorPayload(**step_error))` raises a `pydantic.ValidationError` (2 missing required fields) that escapes `write_node` uncaught, which `core/run.py`'s outer `except Exception` catches and reports only as a generic "This query failed unexpectedly before it could complete." with the real cause fully swallowed (`run()` logs nothing on this path). Blocks 3 of 22 cases in `tests/system_03_search_agent/tools/test_cypher_query_e2e.py` (`test_full_loop_reaches_the_graph_and_returns_a_cited_answer`, `test_full_loop_works_for_a_gene_outside_the_symbol_seed_table`, `test_full_loop_refuses_when_the_graph_returns_nothing`) whenever the guard-tier mock response happens to fail `classifier.parse_classification`; reproduces on unmodified `main`/`HEAD`, not a T-3.4-03 regression. Not fixed here: `guardrail_node` is build phase 3.0 territory, outside T-3.4-03's file scope (`core/graph.py` limited to `_node_or_edge_type_by_citation_id` and its call site). One-line fix once picked up: add `"fatal": True, "scope": "step"` to that dict, or route it through `_step_error_kwargs`-shaped construction instead of a bespoke literal
- F-3.4-T05-01 | closed | Live-found re-verifying T-3.4-05 against the flagship "which diseases are associated with BRCA1?" question: `_node_or_edge_type_by_citation_id`'s `by_identity` dict, keyed only by `(source_url, curie)`, let a "derived" sibling row (produced by a `RETURN d, d.id`-shaped Cypher, sharing the exact same identity as its origin `Disease` row) silently overwrite the real row's correctly-threaded `traversed_edge_type` with an empty value whenever the derived row was iterated second, with no ordering guarantee between the two. Every claim misclassified `low` again, T-3.4-03's own fix never having regressed: `_traversed_edge_type_by_column` still threaded the edge label onto the real row correctly the whole time. Not caused by T-3.4-05's own dispatch: reproduces with `ncbi_efetch` never called, a pre-existing dict-collision bug in T-3.4-03's function, armed only because live Cypher generation happened to produce a colliding shape during this ticket's re-verification. Fixed: never let a "derived" or empty-typed row's entry overwrite an already-informative one for the same identity, order-independent. 2 new regression unit tests (order both ways). `DECISIONS.md`, 2026-08-09
- F-3.4-T05-02 | closed | Live-found via a real failure in `test_write_grounding_premise.py` (an earlier phase's gate, re-run as this ticket's own required regression check): `_known_total_available`/`_ok_finding_was_truncated` read `total_available`/`truncated` across every `"ok"` `Finding` regardless of tool. A dispatched `ncbi_efetch` finding's own `total_available=None` (the normal, non-paginated case; `ncbi_datasets_actions.dataset_report` only ever sets it when that ONE call's own result list was itself truncated) poisoned a KNOWN Layer 1 total into an unknowable one the moment any Gene-anchored query also dispatched Layer 2, downgrading the truncation note from "showing N of KNOWN-M" to "the exact total is not available for this query" even when cypher_query's own total was, in fact, known. `total_available` on a Layer 2 finding answers a structurally different question (was THAT call's own result paginated) than the Layer 1 concept the truncation-note mechanism exists to state (Section 6.1's row_limit/byte-ceiling cut on this query's graph rows); summing or ORing the two was a category error the single-tool design never had to name. Fixed: both functions scoped to `finding.layer == "layer_1_graph"`. 4 new regression unit tests. `DECISIONS.md`, 2026-08-09
- F-3.4-T05-03 | closed | Live-found by instrumenting `run_grounding_pass`'s own input/output per this ticket's explicit troubleshooting instruction, after P1 repeatedly refused with "no statement in the generated answer could be matched" despite a real, citeable Layer 2 finding existing: `_is_vocabulary_token_artifact`, a heuristic built and tuned for one specific defect (a MedGen ETL leak writing a source-vocabulary CODE such as "MeSH"/"SNOMEDCT_US" into a Layer 1 `Disease`/`OntologyClass` row's `name`), false-positived on `ncbi_efetch`'s real, correct `symbol` field ("BRCA1", 5 characters, past `_MAX_PLAUSIBLE_ABBREVIATION_CHARS`), silently deprioritizing it behind `description` (a field that routinely duplicates Layer 1's own `name` text verbatim), which pushed the model toward a redundant citation pair and, on live runs, sometimes toward a hedging sentence about the Layer 2 finding that the grounding pass then correctly stripped as unmatched. Live-verified root cause directly: `_is_vocabulary_token_artifact("BRCA1")` returns `True`; a live probe confirmed the real NCBI Datasets response reliably carries a real, non-blank `symbol="BRCA1"` every time, so the field was always present and always mis-ranked, never missing. Fixed: `_pick_representative_field` gained `apply_vocabulary_artifact_check` (default `True`, every existing caller unchanged), threaded through `synthesis/findings.py`'s `_citable_value_for_row`/`build_synth_findings` keyed on `finding.layer == "layer_1_graph"`. Not a `_MAX_PLAUSIBLE_ABBREVIATION_CHARS` tuning issue: that constant is calibrated against real, confirmed MedGen leaks a wider threshold would let straight through, so this is a scope fix, not a sensitivity fix. `test_findings_degenerate_values.py`'s `_stub` helper updated to accept `**kwargs` (mechanical compatibility only, no assertion changed), since `build_synth_findings` now forwards the new keyword unconditionally whenever a `Finding.layer` is known. Live-verified after the fix: a subsequent run produced a clean, single, correctly Layer-2-cited sentence ("The official gene symbol for NCBIGene:672 is BRCA1 [1]."), and P1 passed on a dedicated re-run and the next full-gate run. `DECISIONS.md`, 2026-08-09
