# Doc readability runs

Evidence log for every run of the `doc-readability` skill, one row per run. Each run has three parts:

- The preservation script
- The style script
- The `doc-auditor` agent's grading round A fresh session cannot otherwise tell whether the gate ever ran on a given document, per `.claude/rules/goal-contracts.md`'s requirement that a verify surface leave evidence rather than a claim. This file is that evidence.

The first real run landed on 2026-08-25 and replaced the seeded example row, as that row's own instruction required.

## Index by date

- 2026-08-25: `docs/build/Build_workflow_cadence.md`, the skill's first real run and its end-to-end proof.
- 2026-08-25: `README.md`, the second run, which calibrated the comma-chain arm against a document that was already close to clean.
- 2026-08-25: a five-document batch run in parallel, one agent per file, after a phase checkpoint: `requirements/Plan.md`, `requirements/phase_6/Continuation_prompt.md`, `PROGRESS.md`, `CLAUDE.md` and `AGENTS.md`. Two locked documents were analysed report-only and never edited, in `tracker/locked_docs_readability_report.md`.
- 2026-09-22: `testing/UI_fix_plan.md`, the second run on this file: the product owner's high-level tracker at the top, both scripts green, stopped at the two-round auditor cap and put to the product owner.

## Table

Each row's detail sits behind a "why it landed here" dropdown in the Notes cell, matching the `<details><summary>` convention `DECISIONS.md` uses, so the table scans as one line per run and you open only the row you care about.

Column meaning:

- Date: the date the run happened, `YYYY-MM-DD`.
- Document: the file path the skill ran against.
- Mode: `optimize` for a restructure of an existing document with a before version, `author` for a freshly written document with no before version.
- Preservation: `check_preservation.py`'s exit code, plus its one-line summary (atoms checked, findings, additions, retention).
- Style: the style script's exit code, plus the defect count before and after the restructure.
- Auditor: the `doc-auditor` agent's overall verdict, PASS or FAIL, plus which round produced it (a FAIL is expected to trigger another round).
- Additions: the count of manifest rows by classification (for example, how many were genuinely new explanation versus how many the auditor caught as reworded prose walls).
- Notes: anything that does not fit the columns above, wrapped in `<details>` so the table stays scannable.

A correction to a premise this log's early rows relied on, recorded rather than quietly fixed. The survey that scoped this skill reported ZERO em dashes across the repository, and that was wrong twice over. What it got wrong:

- Scope: it covered `docs/`, `requirements/` and the root, and never covered `tracker/` at all.
- Accuracy inside its own scope: `LEARNINGS.md` sits at the root and was missed anyway.

The true figure, measured 2026-08-25:

- Total: 11 files carrying 102 em or en dashes
- `LEARNINGS.md`: 68, almost all in section headings
- `tracker/` phase reports: most of the remainder
- Runs through this skill: none of these files, ever

What survives the correction, and what does not:

- Still true: for the documents actually processed by this skill, punctuation linting was not where the value lay. Every one of them was clean on dashes.
- No longer true: the same claim about the corpus as a whole. None of the 11 affected files has ever been run through this skill.

Rows are append-only. A row is never edited or deleted once a real run lands it, the same rule `DECISIONS.md` and `LEARNINGS.md` already follow for their own history. A wrong row is corrected by appending a new row that says so, not by rewriting the old one.

| Date | Document | Mode | Preservation | Style | Auditor | Additions | Notes |
|------|----------|------|---------------|-------|---------|-----------|-------|
| 2026-08-25 | `docs/build/Build_workflow_cadence.md` | optimize | exit 0, 2365 atoms, 0 lost, 0 orphan claims, 0 negation drops, 7 additions, retention 0.999 | exit 0, 20 hard defects before, 0 after | PASS, round 1 | 7 total: 5 first-principles explanation, 2 structural scaffolding, 0 unclassified | <details><summary>why it landed here</summary>The skill's first real run, and it was run as the end-to-end proof of the skill itself rather than because the document was the worst offender. The style gate was observed FAILING first, at 20 hard findings, before any edit. Four defects in the two scripts were found by this run and fixed, which is the reason to record it in detail. One: the claim-anchoring union ranked candidates by raw shared-atom count, so a Mermaid fence carrying fifteen stage numbers outranked the three bullets a sentence had actually been split into, and three legitimate splits were reported as orphan claims. Replaced with greedy set cover over what the before-unit still needs. Two: the candidate pool was rank-truncated at 24, which dropped a bullet holding nothing but `Phase_6_execution_flow.html`, so a claim failed for a fact sitting in the document. Counted-atom holders are now always included. Three: the negation check compared against a single anchor, so a sentence that split in two reported a dropped negation that had merely moved to the sibling bullet. It now counts over sentence fragments. Four: the style script's comma-chain arm counted commas across a whole line rather than within a sentence and required no coordinator, and 10 of its 14 findings on this document were ordinary prose carrying subordinate clauses. Tightened to detect an actual series. One coverage claim was also CORRECTED rather than defended: a cross-unit value swap had been asserted as catchable, and widening the union to stop the false positives gave that up, so it is now asserted as a known miss. The auditor passed all eleven criteria in round 1 and flagged two borderline generalizations in the added explanation, both of which were tightened before this row landed.</details> |
| 2026-08-25 | `README.md` | optimize | exit 0, 1751 atoms, 0 lost, 0 orphan claims, 0 negation drops, 1 addition, retention 1.000 | exit 0, 2 hard and 3 advisory before, 0 and 0 after | PASS, round 1 | 1 total: 1 structural scaffolding, 0 unclassified | <details><summary>why it landed here</summary>Chosen deliberately as a document already in good shape, since a gate is only calibrated once it has been run against work that is nearly correct as well as against work that is not. It started at 2 hard findings against the cadence document's 20, and both of the style script's remaining arms were wrong. One: the comma-chain arm flagged "questions about genes, diseases, variants, publications, and taxonomy", a bare noun list averaging 1.2 words per item, which is ordinary English rather than a list wearing a paragraph. The arm now requires a mean of 4 words per item, the level at which an item carries its own predicate, which is what writing-style.md's own smell test describes. Two: the heading-case arm reported "System" twice in one heading and treated "System 1" as title case. A capitalized word followed by a number is now recognized structurally as a designator, which generalizes to Layer 2, Phase 6 and Section 25 rather than growing an allowlist, and each word is reported once per heading. The real edits were small: one three-item prohibition became a bullet list, and an architecture diagram was added. The auditor passed all ten applicable criteria and correctly marked the first-principles criterion NOT APPLICABLE rather than passing a shape that was never attempted. It also caught a genuine defect in the new diagram, which drew each layer feeding the Write step directly and so implied the layers bypass Act on the return leg. Fixed to round-trip through Act before this row landed.</details> |
| 2026-08-25 | `requirements/Plan.md` | optimize | exit 0, 19814 atoms, 0 lost, 0 orphan claims, retention 1.000 | exit 1, 59 hard before, 42 after, every remaining finding inside the Revision history changelog | PASS, round 2 | 0 additions | <details><summary>why it landed here</summary>The 42 remaining findings are NOT open defects. They sit inside `## Revision history`, which `writing-style.md` sanctions as a dated bullet changelog and explicitly exempts from the wall rule. Verified none leak outside it. Two Mermaid diagrams were attempted and REVERTED, because the file paths they needed could not fit the 30-character label limit and a truncated label is worse than no diagram. One process failure is recorded against the run rather than hidden: round 2's auditor brief disclosed what round 1 had found, which the auditor itself flagged as breaking its own fresh-context contract, so that verdict should be read with the caveat even though the finding it raised was real.</details> |
| 2026-08-25 | `requirements/phase_6/Continuation_prompt.md` | optimize | exit 1, 6 lost atoms and 4 orphan claims, all deliberate supersessions | exit 0, 47 hard before, 0 after | PASS, round 2 | 1 Mermaid diagram | <details><summary>why it landed here</summary>This file was STALE in a way that mattered, and the checkpoint had to run before the restructure or the staleness would have been carefully preserved. Its status line said build phase 4.16's pull request was open and unmerged when it had merged as PR #63, and a later section still said the product owner had not ranked the UI-defects versus CI choice. Every preservation finding is one of those superseded statuses or a corrected count, not a loss. The agent also refused a figure given to it in its own brief: told the skill had found seven gate defects, it checked this log, found the evidence supported six, and corrected the number rather than repeat an unverifiable one.</details> |
| 2026-08-25 | `PROGRESS.md` | optimize | exit 1, 3 lost atoms and 1 orphan claim, all deliberate corrections | exit 0, 34 hard before, 0 after | not dispatched, gates only | 0 additions | <details><summary>why it landed here</summary>All four preservation findings are corrections rather than losses, each verified independently rather than accepted: 'we do NOT yet know the cause' went because the cause was found, 'about sixteen seconds' went because the measured figure is 10.9 and the document now says eleven throughout, and 'four ways of connecting' became five because six delivery surfaces exist and five shipped. The document keeps a paragraph correcting what it itself claimed the previous week, on the stated grounds that a status document which silently rewrites its own past is worth less than one that shows where it was wrong.</details> |
| 2026-08-25 | `CLAUDE.md` and `AGENTS.md` | optimize | exit 0 on both, 2786 and 2791 atoms, 0 lost, retention 1.000 | exit 0 on both, 4 hard before, 0 after | PASS | table of contents added | <details><summary>why it landed here</summary>The target was not in the finding count. The Current focus table's Priority-2 cell was a single table cell of roughly 12,000 characters carrying the whole build narrative, the densest prose block in the repository, and the style gate does not flag it because its wall arm excludes table rows by design. That cell's own text claimed it no longer carried the per-phase narrative while doing exactly that. The narrative moved to a new `## Build phase history` section and the cell now holds a status line plus a pointer, which makes its own claim true. Both files stay byte-identical apart from the title and intro lines, verified by diff.</details> |
| 2026-08-30 | `requirements/phase_6/Continuation_prompt.md` | optimize, STOPPED AT STEP 3 | not run: the run stopped before any edit | exit 0 on the untouched file, 0 hard 0 advisory | not dispatched, correctly | The skill's step 3 says a gate green on first observation means there is nothing to optimize, so stop and say why rather than proceeding. It was green. The document had just been cut from 825 lines to 351 by moving sixteen superseded sections to `Phase_6_history.md`, which is a CONTENT operation this skill explicitly cannot perform: its preservation gate forbids dropping a fact under cover of a formatting pass. So the real defect was fixed by relocation first, committed, and the style gate then found nothing left to do. Recording a stopped run rather than omitting it, so a later reader can tell the gate ran and found the file clean, rather than that it was never run |
| 2026-08-31 | `docs/build/Debugging_guide.md` | author | not run, and stated rather than skipped: author mode has no before version, so there is nothing to prove no loss against | exit 0, 14 hard before the first fix pass, 0 after | FAIL round 1, FAIL round 2, FAIL round 3, all fixed; shipped after round 3 by product-owner decision | the whole document is new: 120 source rows, a 17-row symptom index, one Mermaid diagram, two first-principles sections | <details><summary>why it landed here</summary>The first document in this repository to ship with a MECHANICAL staleness gate rather than a convention: `tests/system_03_search_agent/test_debugging_guide_coverage.py`, three arms plus a mutation case each, riding CI gate 4. Arm 1 earned its place BEFORE the document existed, catching three uncovered files in the assembled worker output, one of them `tools/__init__.py` where the read-plus-one-source tool contract is written down. Arm 2 then caught a phantom path the lead wrote by hand, `src/hooks/useAgentRun.ts` for `frontend/src/hooks/useAgentRun.ts`. THREE audit rounds against a two-round cap, the third authorised by the product owner on falling severity, 30 defects total. THE MOST TRANSFERABLE RESULT IS NOT A BUG: nine of round 2's fourteen defects sat in the ONE section no arm watches, the environment table, and the auditor named that correlation itself. A gate shapes where defects survive, not just whether they are found. THREE separate defects were the lead's own confident sentences: a cell opening "Measured:" whose measurement was falsified by `synthesis/freshness.py`; a stated method, every cell from its file's docstring, that did not hold for the largest file because `core/graph.py`'s docstring still says "stub nodes"; and a symptom row telling readers `/verify` does not run `isort`, which it has since 2026-08-30. The guide's own load-bearing paragraph reproduced a STALE DOCSTRING rather than the call site: `run.py` states `stream_mode="updates"` in two docstrings while the call site passes `stream_mode=["updates", "custom"]`. TWO PROCESS FAILURES ARE RECORDED RATHER THAN TIDIED: round 2's brief told the auditor it was a second round, leaking prior-round knowledge into a context meant to be blind, and the lead edited the document WHILE round 2 graded it. The auditor flagged both itself. Round 3 ran with a clean brief against a frozen file. A mechanical generator for the environment table was written and DISCARDED, because it reported `GUARD_MODEL` and `LANGSMITH_API_KEY` as read by nothing: a mechanically wrong table is worse than a hand-checked one. Separately, this run restored a CORRUPTED HISTORICAL FIGURE in `requirements/phase_6/Phase_6_history.md`: build phase 4.3's close was 3308 Python tests, proven from commit `f52761f`, and commit `5249f36` had overwritten it with the then-current 4652 while relocating the section, which is `goal-contracts`'s never-corrupt-the-subject rule broken in the direction that looks like diligence.</details> |
| 2026-09-20 | `requirements/phase_6/Continuation_prompt.md` | optimize | exit 1, 9 lost atoms and 1 orphan claim, ALL from one deliberate correction | exit 0, 6 hard before, 0 after | FAIL three times, extended past the two-round cap on product-owner instruction, then stopped | 0 additions reported, 3 named by the auditor | <details><summary>why</summary>RUN AGAINST A CAPTURE DOCUMENT BY EXPLICIT PRODUCT-OWNER REQUEST. The skill's own refusal list exempts continuation prompts as capture documents, while its "Why this exists" section cites this exact file as a defect example, 22 prose walls in 621 lines. The skill contradicts itself here; the product owner naming the file and the skill together is the waiver the refusal list asks for. EIGHT WALLS WERE SPLIT at clause boundaries the sentences already had, and the style gate went 6 hard to 0. ROUND 1 FAILED on house style: two walls remained that the style script scored clean, because `check_style.py` evaluates PHYSICAL lines and never joins a wrapped paragraph back together, a limit its own docstring states. A wall hard-wrapped at 80 characters is invisible to it. Both were fixed. ROUND 2 FAILED on two criteria. The first was the lead's own invention and the worse of the two: a bridging sentence added in round 1 to answer round 1's duplication complaint asserted that the list below carried all three named items, and IT WAS FALSE, since the bossman deny-rule item appears only in the list above. A reader trusting it would have concluded the item was dropped. Deleted. The second was five prose walls in text this pass never touched, all pre-existing, three of them in the two sections a fresh session is told to read first. FIXED after the product owner extended the pass past the cap. ROUND 3 THEN FAILED ON A FOURTH TIER, four more pre-existing walls plus one addition of the lead's own: a lead-in sentence reading "Three things about those commands" that was the same shape as the false bridging sentence deleted in round 2, and loose besides, since the first of its three bullets is not about the commands. Cut, because letting it stand while deleting the other would be inconsistent. All four walls were split and the run stopped there, having done exactly what round 3 named as clearing its FAIL. THE PATTERN IS THE RESULT: three auditor rounds each found a DIFFERENT tier of pre-existing wall while `check_style.py` reported 0 hard throughout, because the script measures physical lines and never rejoins a wrapped paragraph. A green script is not evidence of no walls in a hard-wrapped document. Two candidates the round-3 auditor named and deliberately did not fail on are left for the next reader to judge: the UI fix loop's step 5, and the semantic-guardrail intake bullet. The roughly 70-row Open items table is exempt as table content and is, by the auditor's own reckoning, the hardest thing in the file to read. THE PRESERVATION GATE EXITS 1 DELIBERATELY. All 9 lost atoms and the 1 orphan claim trace to a single sentence that was FALSE: line 42 said "TWO WORKTREES STILL EXIST" while line 95 said both were gone, and line 95 was right. Correcting a handover document's false statement outranks a green gate, and reverting the fix to restore retention 1.000 would be the inverse of reward hacking, corrupting a correct subject to satisfy a check. Two of the nine atoms come from that sentence's closing observation about a contradiction in `testing/UI_fix_plan.md`, verified obsolete before it was dropped: that row now reads "DONE, both worktrees are gone".</details> |
| 2026-09-20 | `testing/UI_fix_plan.md` | optimize | exit 0, 11042 atoms, 0 lost, 0 additions, lex retention 1.000 | exit 1, 21 hard before, 6 hard after, ALL SIX DELIBERATE | FAIL, and the failure is the most useful result of the night | 0 additions reported by the script, 2 declared by the lead | <details><summary>why</summary>Run on the product owner's instruction after they called the document very hard to read and named the risk that the next session's agent would be confused by it. TWENTY WALLS WERE CONVERTED and the preservation gate stayed perfect throughout. THE AUDITOR STILL FAILED IT, on criteria 3, 5 and 6, and the finding generalises far past this document: BULLETING WAS OVER-APPLIED, and in six places it detached a qualifier from what it qualified or moved who was acting. "every question searches the knowledge graph, live NCBI records, and literature and trials at the same time" became three bullets in which "at the same time" governed only the third, so a scanner read "literature and trials happen at the same time" rather than all three layers. "fixed by rules in code, never by rewording" became a bullet list whose first two items had no predicate at all and whose load-bearing negation governed only the last. A product owner's quoted ask became three imperatives, moving the actor from the lead scientist to the reader. All six were reverted to their original sentences. THE POINT IS THAT BOTH SCRIPTS WERE GREEN THE WHOLE TIME: preservation read 0 lost and retention 1.000 because every word survived, and the style gate read the bulleted versions as clean. Meaning changed while both gates said nothing had. A lexical no-loss check cannot see an attribution swap, and its own docstring says so. THREE HEADING PROMOTIONS WERE ALSO REVERTED: promoting a prose label that introduces a table silently widens its scope to everything before the next heading, so "Being built right now" came to contain a table whose own rows say nothing is being built. SIX HARD FINDINGS STAND, and every one is a case where obeying the checker makes the document worse: five are the reverted sentences the auditor said must not be bulleted, and one is an en dash in `"1–13"` that quotes verbatim what `CitationMarkers.tsx` line 141 renders and two tests pin. Fixing any of them would corrupt a correct subject to satisfy a check. TWO ADDITIONS ARE DECLARED RATHER THAN SMUGGLED: an HTML comment beside the dash recording why it stands, and the lead's own rewrite of the "What is waiting on the product owner" table, which said "Three things" above four rows and whose four rows had all since been resolved.</details> |
| 2026-09-22 | `testing/UI_fix_plan.md` | optimize | exit 0, 13888 atoms, 0 lost, 0 orphan claims, 0 negation drops, lex retention 1.000, re-run after every addition | exit 0, 9 hard before (8 comma chains, 1 dash), 0 after | FAIL twice; stopped at the two-round cap and put to the product owner rather than a third round | 0 reported by the script, 16 declared by the lead, all structural scaffolding or a table-of-contents entry | <details><summary>why</summary>Run on the product owner's instruction, in their words "I have no idea of what features are done and what is left", with an explicit ask: a high-level tracker right at the top, features to implement, features done, additional notes. THE TRACKER WAS BUILT FROM THE DOCUMENT'S OWN STATUS WORDS, and the script's additions arm reported no rows because every new unit reuses phrases the file already carries, so the lead declared the sixteen added units by hand. ROUND 1 FAILED on criteria 6 and 7: the tracker's "approved except where the cell says otherwise" sentence contradicted 8.4's own row, an open defect (L-01) was absorbed into a closed-sounding bucket, a date the document does not carry entered a comment, and five walls remained. All fixed but one. The auditor also named two dispatch leaks, a mention of the script's additions arm and a statement of the run's goal, corrected for round 2. ROUND 2 FAILED on criteria 4 and 7: a lead-in of the lead's own making governed bullets it did not describe (fixed, two instances), and six more walls. Four were converted; TWO WERE REVERTED because the preservation gate's claim arm anchors a before-sentence to a union of at most three after-units, so a six-item enumeration cannot become six bullets under it, and a table collapses repeated atoms. THE ONE FINDING NEITHER ROUND COULD CLOSE: the 11.33 table cell, roughly 2,900 characters, which the document's own editing rule says belongs in a Detail subsection. Moving it makes the cell an orphan claim, because a cell-sized unit anchors only to another cell-sized unit while prose is split into sentences. Left in place and reported. Left for the product owner: the table of contents now sits after three content sections, by their instruction that the tracker come first; the tracker is a third status index that can drift from the rows it summarises; the Set 11 table carries two rows for 11.34.</details> |
| 2026-09-23 | `testing/UI_fix_plan.md` | optimize | exit 0, 19905 atoms, 0 lost, 0 orphan claims, 0 negation drops, lex retention 1.000, re-run after every change. Three sentences split into bullets produced orphan claims and were put back as written rather than loosening the gate | exit 0: 7 hard before (comma chains), 0 hard after; 15 advisory heading-case notes on item titles that name on-screen buttons, untouched | FAIL twice; stopped at the two-round cap and put to the product owner. Round 1: retests filed under Done, an invented priority ranking, dense sentences. All three fixed: retests lead the To do table, the ranking follows the plan's own "Next, in order". Round 2: the provenance note recording the owner's instruction cannot be verified from the before file (it records this session's instruction), and dense sentences remain elsewhere in the document, three of which the no-loss gate will not let be split. Round 2 also noted that it was given the owner's target structure, which is outside its allowed inputs | 1 reported by the script, 7 declared by the lead (provenance note, two ordering notes, the Priority column, the legend and its retest entry, and a repair: 12.11's last cell had been appended to 12.12's row on 2026-09-23 and was moved back verbatim) | The owner's structure: 1. being built now, in priority order; 2. to do, in priority order; 3. done. The table of contents moved up to follow the intro |
| 2026-09-24 | `testing/UI_fix_plan.md` | optimize, round 3, approved by the product owner and run by a background agent | exit 0, 21419 atoms, 0 lost, 0 orphan claims, 0 negation drops, 0 additions, re-run after every split and after the lead's two fixes | exit 0 before and after, 0 hard | FAIL on criterion 7 only, 10 of 11 pass. Three regions changed, the lead's own review added one capital letter and one blank line (a sentence would have rendered glued onto a list), and the auditor confirmed every word of the three is the before's own. The FAIL names 12 dense paragraphs this round did not reach, all written before it, three of which the no-loss gate has already refused to let be split. Not run a fourth time without the product owner | 0 | The top tracker, every quoted line and every table row were proven byte-identical by diff. The 11.21 note was left as written because it sits inside the tracker, which the product owner ruled must not change. The auditor also noted, outside its checklist and pre-existing: a heading one level too high (`### 11.31 resolved`) that mis-nests two detail sections, and a "VERIFY THE MARFAN PHENOTYPE QUESTION" line that renders inside the 12.4 bullet |
| 2026-09-24 | `testing/UI_fix_plan.md` split into itself and `testing/UI_fixes_done.md` | optimize, a split: one before, two afters graded together, on the product owner's instruction to keep what is being built and what is next apart from what is done | exit 0 on the two afters concatenated against the frozen before: 21419 atoms, 0 lost, 0 orphan claims, 0 negation drops, re-run after every fix | exit 0 on both files, 0 hard | FAIL twice; stopped at the two-round cap and put to the product owner. Round 1: five pointers the move made false, seven stale statuses copied into the done index, one wrong retest mapping, the meaning of "done", and two mis-nested headings, all fixed. Round 2: criteria 1 to 5 pass, nothing lost; criterion 6 fails on the done index's test-query and retest-item columns, which come from `testing/Test_queries_and_workflows.md` and the shipped lists by the product owner's own instruction, and on four sentences the lead wrote that overstated the before, corrected after the round; criterion 7 fails on dense paragraphs carried over unchanged | The lead declared every addition; the script found none it could not anchor | Built by two parallel agents from one frozen copy, one per file, from a line-by-line manifest. 2572 lines became 766 and 2002 |
