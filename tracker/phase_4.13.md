# Build phase 4.13: durable cross-reload search history

Branch: `phase/4.13-durable-history`
Depends on: 4.6 (the `interactions` substrate), merged 2026-08-21 as PR #54
Opened: 2026-08-27
Status: OPEN

Inserted on the board 2026-08-21 by product-owner decision to settle a scope conflict rather than leave it: `frontend/src/stubs/registry.ts` named build phase 4.6 as history's owner in two notes written by build phases 4.8 and 4.10, while Section 25's row for 4.6 never named history as a deliverable. Section 25 does not contain this phase.

## Table of contents

- [What this phase is for](#what-this-phase-is-for)
- [What the substrate already provides](#what-the-substrate-already-provides)
- [The scope boundary, decided before any ticket](#the-scope-boundary-decided-before-any-ticket)
- [The critical this phase can ship, named before it is written](#the-critical-this-phase-can-ship-named-before-it-is-written)
- [Tickets](#tickets)
- [Findings](#findings)
- [Coverage: what this phase does not cover](#coverage-what-this-phase-does-not-cover)
- [History](#history)

## What this phase is for

A person asks a question, closes the browser, comes back, and their searches are gone. Today the rail's items are React state in `frontend/src/App.tsx` (`const [history, setHistory] = useState(...)`, line 166) and nothing reads them back. Build phase 4.6 built the substrate that makes them durable and stopped there: rows exist, they are owner-scoped, and nothing queries them.

This phase adds the read path and the UI that renders it.

## What the substrate already provides

Read rather than assumed, before any ticket was written:

- `interactions` carries `owner_id`, the exact namespaced principal (`user:<uuid>` or `guest:<uuid>`), added by alembic 0008 for F-4.6-01. It is `NULL` on any row written before that column existed.
- `feedback/writer.py`'s `_caller_owns_row` is the ownership check this phase copies: one direct `row.owner_id == owner_id` compare, plus a `is not None` guard so a pre-migration row is refused rather than guessed at. It reads the fact, not a proxy for it.
- `reassign_interaction_owner` already re-points a guest's captured rows to an account on sign-in, so "your searches move with you" needs no new mechanism here.
- `Principal.owner_id` is already on every authenticated request (`get_caller`), and `GET /v1/allowance` is the shape a new owner-scoped GET follows.

## The scope boundary, decided before any ticket

The `interactions` row stores no answer narrative. It carries `query_text`, `citations`, `trust_signal`, `rubric_outcome`, `query_class`, timings and cost, and no synthesised text (`feedback/contracts.py`, `InteractionRow`).

So this phase makes the LIST of past questions durable, not the answers. Clicking a restored item re-asks the question, which is exactly what the rail already does for a live item today. Persisting answer text is a new column, a new migration and a retention decision about storing generated biomedical prose, none of which this phase's board row asked for. Recorded as decision D-4.13-01 rather than silently narrowed.

## The critical this phase can ship, named before it is written

Build phase 4.5 already shipped this exact defect once: every guest shared one ownership identity because `user_id` is `NULL` for a caller with no account, so any anonymous caller could read and overwrite any other guest's session memory. A read path over `interactions` is the same shape with a wider blast radius, because it returns a list of other people's questions rather than one memory envelope.

The rule for every ticket here: scope by `owner_id`, exact string equality, in the SQL. Never by `user_id`, never by `session_id`, never by a derived or "equivalent" identity.

## Tickets

| Ticket | Deliverable | Files it may touch | Acceptance criteria | Status | Owner |
|--------|-------------|--------------------|---------------------|--------|-------|
| T-4.13-01 | The owner-scoped read function over `interactions` | `src/system_03_search_agent/feedback/history.py` (new), `tests/system_03_search_agent/feedback/test_history.py` (new) | `list_history(owner_id, limit)` filters on `Interaction.owner_id == owner_id` in the SQL with a real `LIMIT`, never a Python slice; orders `created_at DESC, id DESC` so the order is total and stable; refuses an empty or over-long `owner_id` rather than querying; a `NULL`-`owner_id` row is never returned; no f-string or `.format()` in any query; type hints on every signature | in-progress | builder-backend |
| T-4.13-02 | `GET /v1/history`, the endpoint | `src/system_03_search_agent/adapters/web_sse/app.py`, its response model beside the other adapter models, `tests/system_03_search_agent/adapters/` | Pydantic response model with `max_length` on every string and `max_items` on the list, per the multi-agent pipeline gate; `limit` validated `ge=1, le=50`, default 20; 401 with no token; accepted for both a guest and an account principal; two principals in one database never see each other's rows; the error message on a rejected limit says what to send instead | in-progress | builder-backend |
| T-4.13-03 | The frontend read and render | `frontend/src/lib/api.ts`, `frontend/src/App.tsx`, `frontend/src/components/answer/FollowUp.tsx` | `fetchHistory` typed like its siblings; the rail is seeded once a principal exists and again after sign-in; a run taken this session is not duplicated by its own captured row on the next fetch; clicking a restored item re-asks it; the empty state no longer claims searches only last "in this session"; a failed or 401 fetch leaves the live list on screen rather than blanking the rail | in-progress | builder-frontend |
| T-4.13-04 | The premise gate, written before the builders and watched failing | `tests/system_03_search_agent/adapters/test_phase_4_13_premise.py` (new) | Every arm carries a populate-check that can tell the control holding from nothing having happened; arms cover cross-principal isolation for two guests and two accounts, the `NULL`-`owner_id` refusal, durability across a fresh client with the same token, the guest-to-account handover, and the limit bound; the file states its own coverage and what it deliberately omits | done | lead |
| T-4.13-05 | Tests for what shipped | `frontend/src/*.test.tsx`, `frontend/e2e/` | Vitest covers seed, merge-without-duplicate, and the degraded-fetch path; a browser test proves the reload path if the e2e mock can reach it, and says so explicitly if it cannot rather than asserting a weaker thing quietly | in-progress | builder-frontend |
| T-4.13-06 | Docs and the record | `frontend/src/stubs/registry.ts`, `DECISIONS.md`, `tracker/BOARD.md`, `LEARNINGS.md` | The `history` stub entry becomes real, with the answer-not-restored boundary stated rather than dropped; both decisions logged; the board row closed with evidence | todo | lead |

## Findings

| ID | Raised by | Severity | Description | State | Reason | History |
|----|-----------|----------|-------------|-------|--------|---------|
| F-4.13-01 | lead, by mutation | minor | Two clauses claimed the read order is TOTAL and neither could fail on it. The premise gate's `test_the_list_is_newest_first_and_the_order_is_total` stayed green 10 of 10 runs with the `id` tiebreaker deleted from the ORDER BY, because rows written through separate transactions never collide on `created_at`, so the tie the tiebreaker exists for is never reached. The unit clause `test_order_is_total_when_created_at_collides` forced the collision correctly but used TWO rows, where a tiebreaker-less order matches the predicted one half the time: measured 3 of 10. A gate that fires on a coin flip is worse than no gate, because a real finding is then indistinguishable from noise | CLOSED, verified by judge | Neither clause was wrong about the requirement, only about its own reach. Caught by mutation, not by reading, which is the eleventh instance of that in `LEARNINGS.md` | 2026-08-27 lead: raised from a five-mutation sweep of the gate (4 of 5 caught, this one not). 2026-08-27 lead: unit clause raised to six tied rows, moving an accidental match from 1 in 2 to 1 in 720, re-measured at 10 of 10. Premise-gate arm renamed to `test_the_list_reads_newest_first` and its docstring and the file's coverage statement corrected to claim only what it proves, with a pointer to the clause that owns the rest. Raiser does not close: the judge verifies. 2026-08-27 judge: VERIFIED and CLOSED, re-measured from fresh context rather than accepted. Dropping the `id` tiebreaker is now caught 10 of 10 (`M4-drop-id-tiebreaker: CAUGHT 10/10`), by exactly one clause and the one the corrected docstrings name (`FAILED tests/system_03_search_agent/feedback/test_history.py::test_order_is_total_when_created_at_collides / 1 failed, 31 passed`), while the renamed premise-gate arm correctly stays green under that same mutation and does go red under a reversed sort (`M3-order-ascending: CAUGHT 1/1`), which is exactly what it now claims. Two residues of the same overclaim survived the fix and are filed as F-4.13-J-01 and F-4.13-J-02 |
| F-4.13-02 | lead | major | The phase's own done-when is that a person's searches survive closing the browser. On the server they do. On screen they do not, because the browser forgets who the person is: `App.tsx:208` holds the access token in `useState<string \| null>(null)`, so every reload signs an account out, and the rail is gated on `signedIn`. A guest's token DOES persist, but `railAvailable = signedIn && ...` means a guest is shown no rail at all. So after a reload every visitor must sign in again before any history appears. The backend has issued a rotating refresh token since build phase 1.1 (`auth_sessions`, rotation, absolute TTL, revocation) and `LoginResponse` returns it; the frontend receives it and discards it | open, OWNED BY ITS OWN PHASE | NOT fixed unilaterally, deliberately. Where a bearer token is persisted is a security decision with a real trade-off (localStorage is XSS-reachable; an httpOnly cookie needs a backend change, since the token is returned in a JSON body today), and `ai-security-standards` plus this harness's own Ask list both put auth configuration behind human approval. It is also a feature in its own right, "keep me signed in", larger than this phase's board row | 2026-08-27 lead: found while checking a remark the frontend builder made in passing. Verified rather than assumed: grepped every `localStorage`/`sessionStorage` write in `frontend/src`, the only persisted credential is the guest token (`lib/guestSession.ts`), and the access token appears in React state alone (`App.tsx:208`, `:705`, `:935`). The premise gate does not cover this and now says so: it holds one token in a Python variable across its two clients, which is exactly what no browser does. 2026-08-27 product owner: DECIDED, build phase 4.13 merges with this open and "keep me signed in" becomes its own scoped piece of work, where the localStorage-versus-httpOnly-cookie trade-off is settled properly rather than in the last hour of an unrelated phase. Three alternatives were put and declined: persisting the refresh token in localStorage now (cheapest, but makes a bearer credential XSS-reachable), an httpOnly cookie now (safer, but needs a backend change, CSRF handling and a CORS review across two origins), and dropping the `signedIn` gate so guests see their own rail (narrower, but diverges from the prototype and leaves accounts still losing their session) |
| F-4.13-A-01 | adversary | major | LIVE-REACHABLE. `GET /v1/history` is the only owner-scoped route on this surface that admits a guest principal WITHOUT re-checking `guest_sessions`. `get_caller` verifies the JWT signature and nothing else, so a guest identity the server has already revoked at signup, or one whose `guest_sessions` row never existed or was deleted, still authenticates and still gets a 200 answer about that principal's stored questions. Every sibling route refuses the same token. Measured against the same credential in one run: `GET /v1/allowance` returns `401 {"reason":"guest_session_revoked",...}` while `GET /v1/history` returns `200 {"items":[{..."question":"GHOST GUEST QUESTION"...}],"count":1}`. The blast radius is small TODAY only because a revoked guest's rows were moved off `guest:<id>` by `reassign_interaction_owner`, and that is a coincidence of ordering rather than a control: any row written under that owner afterwards is returned to the revoked token, measured directly (`old guest token after a new row lands under it -> 200 ['POST-HANDOVER GUEST ROW']`). A revoked credential that still answers questions about a principal is an authorization defect independent of how much it currently discloses | open | Reachability is LIVE, triggered and pasted, not reasoned. The phase brief named `owner_id` scoping as the critical and got it exactly right; this is one layer above it, at whether the caller was entitled to claim that `owner_id` at all, which no ticket's acceptance criteria mention. `_GUEST_SESSION_NO_LONGER_VALID_DETAIL` in `app.py` documents itself as shared by `POST /v1/query` and `GET /v1/allowance` so the enforcement path and the reporting path cannot disagree; this phase added a third reader of guest identity and did not join that pair | 2026-08-27 adversary: reproduced with a minted-but-unregistered guest token against the real app over ASGITransport and the real `search_agent_users` database. Finder does not close. 2026-08-27 judge: INDEPENDENTLY REPRODUCED from fresh context by deleting the `guest_sessions` row for a live guest and re-reading with the same credential: `GET /v1/allowance -> 401 {"reason":"guest_session_revoked"}` and `GET /v1/history -> 200 {"items":[{..."question":"GHOST GUEST QUESTION 664d3b"...}]}`. CONFIRMED reachable major, and it is THE BLOCKER for this phase: `get_caller` verifies the JWT signature only, the `guest_sessions` liveness check is written inline per route, and this phase added the third reader of a guest identity on this surface without joining that pair  2026-08-27 lead: SCOPE CORRECTED, and it changes the fix. Both the adversary and the judge read this as a gap unique to `/v1/history`, the judge writing that the phase "added the third reader of a guest identity on this surface and did not join that pair". Measured instead of assumed: `get_v1_persona`, `get_v1_query_events`, `post_v1_query_stop`, `get_v1_query_citations` and `post_v1_query_feedback` contain ZERO references to `GuestSession` between them. Only `/v1/allowance` and `POST /v1/query` re-check liveness, so this is a PRE-EXISTING class of six and `/v1/history` is its sixth instance rather than its first. That does not lower the severity of the shipped behaviour, and it does raise the stakes on the fix: adding a check to this one route would be exactly the enumeration Rule 2 forbids, and would leave five siblings. The category fix belongs in the guest branch of `get_caller`, which is shared auth affecting every route on the surface, so it is escalated rather than taken autonomously. |
| F-4.13-A-02 | adversary | major | LIVE-REACHABLE. One row that violates the response model returns 500 for the caller's ENTIRE history, permanently, with no partial degradation and no way for that person to recover. `get_v1_history` builds every `HistoryItem` eagerly inside the response construction, so a single `ValidationError` escapes the handler rather than dropping the offending row. Triggered: with two well-formed rows plus one whose `query_text` is 3000 characters, `GET /v1/history` returned `500 Internal Server Error` and both good rows were lost with it. The bound is set exactly equal to the write-path bound (`question` `max_length=2000`, `CreateRunRequest.text` `max_length=2000`) with ZERO headroom, the database column is unbounded `Text`, and `\d interactions` shows check constraints on `query_class`, `rubric_outcome`, `rubric_score` and `trust_signal` but NONE on `query_text` or `trace_id` length, so the only thing standing between the product and a permanent 500 is that no writer has yet been widened. `feedback.history` reads the same rows with no bound at all, so the refusal exists only in the adapter. The frontend's own `fetchHistory` does the OPPOSITE of the backend for the same hazard, dropping a malformed item so one bad row does not blank a rail; the server has no such rule | open | Labelled LIVE-REACHABLE for the BEHAVIOUR, which was triggered and pasted. The row that triggers it is not producible through any shipped surface today (`web_sse`, `graphql` and `mcp` all bound the question at 2000) so the trigger is a database write or a future widening. That is the point: this is a class of failure whose only remaining guard is an accidental exact-equality between two numbers in two different modules, neither of which points at the other. Checked and clean: the 500 body is the bare string `Internal Server Error`, no DSN, no `search_agent_users`, no bound parameters, so build phase 4.3's disclosure critical did NOT recur here | 2026-08-27 adversary: triggered by inserting one over-long row beside two good ones and re-reading through the endpoint. Finder does not close. 2026-08-27 judge: BEHAVIOUR CONFIRMED, SEVERITY RE-LABELLED to major/LATENT rather than LIVE-REACHABLE, so the merge bar is applied to the same standard as every other finding here. The finding's own reason cell concedes no shipped surface can write the triggering row, so the trigger is a database write or a future widening, not a caller. Verified independently that the exact equality holds today and no write path can breach it: `InteractionRow.query_text` is `max_length=2000` (`feedback/contracts.py:180`), the degraded path substitutes the constant `PAYLOAD_DROPPED_TEXT`, and `_storable`'s `_REPLACEMENT_CHARACTER` is a single code point so no substitution can lengthen a value. The fragility the finding names, two bounds in two modules that do not reference each other with no database constraint behind either, is real and worth fixing |
| F-4.13-A-03 | adversary | major | LIVE-REACHABLE. `interactions.owner_id` carries no index, so the read path this phase shipped costs a full sequential scan plus a sort of the entire append-only capture table on EVERY call. `EXPLAIN ANALYZE` on the exact query `list_history` issues: `Seq Scan on interactions ... Rows Removed by Filter: 21958`, then `Sort Key: created_at DESC, id DESC`. `\di interactions*` shows exactly two indexes, `interactions_pkey` and `interactions_trace_id_key`, neither usable here. The cost of one caller's request therefore scales with every row every user has ever had captured, system-wide, not with that caller's own rows, and `interactions` is append-only by Section 16 with no retention policy. Measured unthrottled: 40 sequential calls in 0.24s, 6.1 ms each, no 429 at any point, on a table of 21958 rows. A guest token is free and self-service from `POST /auth/guest`. `.claude/rules/tool-call-budgets.md` requires a declared bound on every call path; the `LIMIT 50` bounds the ROWS RETURNED and bounds nothing about the work done to find them | open | Not a rate-limiting finding, which is build phase 6.0's work and correctly absent here. The defect is that the query's cost is unbounded in a dimension no caller controls and no ticket named. Nothing in T-4.13-01's acceptance criteria mentions an index, and the `list_history` docstring argues at length for a real SQL `LIMIT` over a Python slice on exactly this reasoning, then does not carry it through to how the rows are found | 2026-08-27 adversary: measured with EXPLAIN ANALYZE against the live `search_agent_users` database and by timing 40 unthrottled calls through the app. Finder does not close. 2026-08-27 judge: CONCLUSION CONFIRMED, EVIDENCE CORRECTED. The finding reports exactly two indexes on `interactions`; there are seven (`interactions_pkey`, `interactions_trace_id_key`, `idx_interactions_created_at`, `idx_interactions_user_id`, `idx_interactions_rubric`, `idx_interactions_coverage_tags`, `idx_interactions_query_trgm`). None is on `owner_id`, so the conclusion stands unchanged and the plan is as claimed: `Seq Scan on interactions ... Filter: (owner_id = 'guest:does-not-exist'::text)` under `Sort Key: created_at DESC, id DESC`, on 22569 rows. Correcting the evidence rather than the conclusion, since a wrong supporting fact in a right finding is what gets the finding dismissed later |
| F-4.13-A-04 | adversary | major | LIVE-REACHABLE. The guest-to-account handover now RENDERS one person's biomedical questions to a different person. A guest identity is browser-scoped, is deliberately not cleared at sign-out (F-4.10-A-05, product-owner decision 2026-08-15), and `_migrate_guest_session` hands every row under it to whoever signs up next while holding it. Reproduced end to end through real endpoints only: person A mints a guest identity and asks `PERSON A: am I a BRCA1 carrier and what is my prognosis`; person B, at the same browser, signs up; the server logs `guest session migrated to a new user; 0 live run(s) and 1 captured row(s) reassigned`; `GET /v1/history` with person B's own access token returns person A's question. The reassignment itself is build phase 4.6's, and it is not new. What is new is that until this phase NOTHING READ THOSE ROWS BACK, so the transfer was invisible bookkeeping; this phase turns it into a durable list on screen, which is the difference between a latent data-transfer and a disclosure | open | The phase's stated critical was one caller reading another's `owner_id`. That control holds (see the clean sweep recorded against A-08's sibling probe). This is the same outcome reached without ever violating it, because the server correctly believes the rows belong to the reader. A control cannot be blamed for an identity that was handed over upstream, which is exactly why this needs to be a product-owner decision about what handover should do rather than a code fix in `history.py` | 2026-08-27 adversary: reproduced with `POST /auth/guest`, a seeded guest row, `POST /auth/signup` carrying that guest token, `POST /auth/login`, then `GET /v1/history`. Finder does not close. 2026-08-27 judge: INDEPENDENTLY REPRODUCED through the same real endpoints: `signup(with A's guest token) -> 201`, then `person B GET /v1/history -> 200; sees A's question: True`, `items: ['PERSON A am I a BRCA1 carrier 4c8d29']`. CONFIRMED reachable major. Agreed that this belongs to the handover rather than to `history.py`: the read path is behaving correctly on rows the server believes are the reader's |
| F-4.13-A-05 | adversary | major | LATENT (reasoned from the code, not triggered in a browser). The phase's done-when is unreachable for EVERY class of visitor, and the two halves of the reason are recorded in two different places so neither reads as fatal. F-4.13-02 names the first half: an account loses its access token on reload, because it lives in `useState` alone. The second half is not named as a defect anywhere: the ONLY principal whose identity does survive a reload is a guest, and a guest is excluded twice over, once by `railAvailable = signedIn && ...` (`App.tsx:409`) and again by the seeding effect's own `if (token === null) return undefined` (`App.tsx:366`), which gates on the ACCOUNT token so `fetchHistory` is never called for a guest at all. The Coverage section states the rail-is-account-only choice as deliberate and matching the prototype, and it is, but it does not state that it closes the last remaining route to the done-when. So the shipped product satisfies the phase's own one-sentence goal for nobody: accounts keep the rail and lose the identity, guests keep the identity and get no rail | open | This is the finding the brief asked for, the claim tested as a person experiences it rather than as the suite frames it, and it is deliberately NOT a re-file of F-4.13-02, which covers only the account half. The composition is what matters: two individually-defensible scoping decisions, recorded in two sections that do not reference each other, that together mean no visitor experiences the feature. Neither the premise gate nor the vitest suite can see it, because the gate holds one token in a Python variable across two clients and the vitest suite drives `signedIn` directly | 2026-08-27 adversary: derived by reading `App.tsx:335-374` and `:409` against the Coverage section and F-4.13-02. Not triggered in a browser; labelled LATENT rather than blurred. 2026-08-27 adversary: the product-owner decision appended to F-4.13-02 while this was being written declines "dropping the `signedIn` gate so guests see their own rail" as one of three alternatives, so the guest half WAS weighed. This finding is filed anyway and narrowed to what that decision does not reach: `railAvailable` is not the only gate, the seeding effect's own `token === null` guard at `App.tsx:366` is a second one that would still have to move, and neither the Coverage section nor F-4.13-02 states the composed outcome, that the done-when currently holds for no visitor at all. Finder does not close |
| F-4.13-A-06 | adversary | minor | LATENT. The Coverage section makes a factual claim about the shipped product that the shipped product does not honour: it states the read path `is capped at 50 rows and a person with more than 50 searches sees their 50 most recent`. `App.tsx:368` calls `fetchHistory(token, { signal: controller.signal })` with no `limit`, `api.ts`'s `fetchHistory` emits a query string only when `options.limit != null`, and the endpoint's default is `DEFAULT_LIMIT = 20`. So a person with more than TWENTY searches sees their twenty most recent, not fifty. The number a reader of the tracker would use to reason about the gap is wrong by a factor of 2.5, and it is wrong in the section whose whole job is making the gap arguable | open | Filed against the coverage statement rather than the code, because either number is a defensible product choice and only one of them can be the one written down. `goal-contracts` requires a verify surface to state its own coverage; a coverage statement that misstates the shipped behaviour is worse than none, since it is the artifact the next reader trusts instead of re-deriving | 2026-08-27 adversary: read off `App.tsx:368`, `api.ts`'s `fetchHistory` query-string construction, and `feedback/history.py`'s `DEFAULT_LIMIT`. Finder does not close |
| F-4.13-A-07 | adversary | major | LATENT. Asking a question that is already in the RESTORED list produces no new rail entry, and relabels the old entry with the CURRENT run's numbers. `ask` dedups on question text (`App.tsx:512-516`: `current.some((item) => item.question === question) ? current : [{...}, ...current]`), a rule that was harmless while the rail held only this session's runs and is not harmless now that it holds up to twenty rows from previous days. Then the meta effect (`App.tsx:448-461`) `.map`s EVERY item whose `question` matches and writes `view.meta` onto it, so a restored row representing a run from an earlier day starts displaying this run's source count and timing. That is the F-4.8-J-01 fabrication class by a new route: the rail states something about a past run that is not true of that run. Three consequences compose: the new search never appears at the top of the rail, `activeId` (`App.tsx:1000`) highlights a row far down the list instead, and the old row's `meta` is a claim about a different run | open | LATENT and labelled so honestly: derived by reading the three reducers against each other, not triggered in a browser, because filing this would have meant adding a test file to the product tree and the brief forbids touching any file but this one. The line numbers are exact and the sequence is deterministic, so it is cheap to confirm. Note the traceId tagging at `App.tsx:609-615` is CORRECT here and guards itself with `item.traceId === undefined`; it is the two older reducers, written when the rail was session-only, that this phase changed the meaning of without changing them | 2026-08-27 adversary: derived from `App.tsx:448-461`, `:512-516`, `:609-615` and `:998-1002`. Finder does not close  2026-08-27 fix agent: REPRODUCED, filed latent and confirmed real. A failing test was written first and shown red against the unfixed code. `ask()`'s dedup added an entry only when the question text was absent, so a re-ask of a listed question neither added nor moved a row, and a later effect then overwrote that row's meta with the new run's numbers. Fixed to filter the old entry out and unshift a fresh one, matching the prototype's `start()`. Two tests added. Lead re-ran the suite independently: 231 passed, typecheck and build clean. Raiser does not close: round 2 verifies |
| F-4.13-A-08 | adversary | minor | LIVE-REACHABLE. HTTP parameter pollution on `limit`: only the LAST occurrence is validated and applied, and the earlier ones are discarded unvalidated. Measured against a caller holding six rows: `?limit=1&limit=50` returned 6 items, `?limit=50&limit=1` returned 1, `?limit=abc&limit=2` returned 200 with 2 items while `?limit=2&limit=abc` returned 422. A differently-shaped parameter is silently ignored rather than refused: `?limit[]=5`, `?limit=%2b5` and `?LIMIT=99` all returned 200 on the default of 20. NO CAP BYPASS WAS FOUND: `le=50` is enforced on whichever value wins, so this does not let a caller exceed `MAX_LIMIT`. It is filed because any component that reads the FIRST occurrence, a proxy, a log line, a future WAF rule, or a hand-built URL, disagrees with the server about what was asked for, and because a request carrying an unparseable `limit` can be made to succeed by appending a second one | open | Over-reported deliberately per the brief. Impact today is a consistency gap, not a control failure. The refusal message itself is good and meets T-4.13-02's acceptance criterion: `Input should be less than or equal to 50` names the bound, so a caller is told what to send instead rather than being silently clamped | 2026-08-27 adversary: measured across 18 `limit` spellings against the live endpoint, including `-1`, `0`, `1.5`, `abc`, empty, `51`, a 26-digit integer, `0x10`, `1e2`, `NaN`, `Infinity` and a trailing NUL, all of which refuse correctly. Finder does not close |
| F-4.13-A-09 | adversary | minor | LIVE-REACHABLE. `count` is `len(entries)`, which is always exactly `len(items)`, so the response carries no signal at all about whether the list was truncated. Combined with no cursor and a default of 20, a person with 21 captured searches is shown 20 and NOTHING anywhere, in the payload or on screen, says a twenty-first exists. A client cannot distinguish `you have exactly twenty searches` from `here are twenty of your searches`, and the field's name invites the first reading. Verified live: with six rows and `?limit=1` the response was `count: 1`, not `count: 6` | open | The Coverage section declares that pagination is out of scope and that is accepted. This is a narrower point that pagination's absence does not cover: a response that omits a total is a scoping choice, but a response that ships a field NAMED `count` whose value is the page size, while omitting the total, is a scoping choice that actively misleads. One integer, `SELECT count(*)`, or dropping the field, would settle it either way | 2026-08-27 adversary: measured against a seeded six-row principal at `?limit=1`. Finder does not close |
| F-4.13-A-10 | adversary | minor | LATENT. `asked_at` is queried, bounded, serialised, typed in the frontend interface, and then discarded: nothing renders it. A restored row therefore arrives on screen as bare question text with no date and no `meta`, which makes it indistinguishable from a live row, from another restored row carrying the same question, and from a row captured months ago. The one piece of information that would tell a person which of two identical-looking entries is which is fetched over the wire on every call and thrown away. `api.ts`'s own comment concedes this (`read only for possible future display; nothing in this phase's UI renders them yet`), so the gap is known; what is not recorded anywhere is that it is what makes the duplicate-question case in F-4.13-A-07 invisible to the person looking at it | open | Minor on its own and filed mainly because it is the reason A-07 is silent rather than obvious. Either render it or stop fetching it; carrying a field the UI cannot use is how a bound gets stale without anyone noticing | 2026-08-27 adversary: read off `FollowUp.tsx`'s `HistoryRail` item render, which uses `item.question` and `item.meta` only, against `api.ts`'s `HistoryItem`. Finder does not close  2026-08-27 fix agent: REPRODUCED, failing test first. `mergeServerHistory` built restored entries with no `meta` at all. `formatHistoryMeta` now renders the closest honest substitute the endpoint actually returns for the prototype's tool/layer/source meta line, `citation_count` plus a short date, and omits the date entirely when `asked_at` is absent or unparseable rather than rendering `Invalid Date`. The divergence from the prototype's exact content is documented at the call site rather than left for a reader to notice. Three tests added. Raiser does not close: round 2 verifies |
| F-4.13-J-01 | judge, by grep | minor | LATENT. `feedback/history.py:112-113` points a reader at `test_the_list_is_newest_first_and_the_order_is_total` and claims it "exercises directly" the total-order property. That arm was renamed out of existence by ebb62f4 (`grep -rn` across the repository finds the name only inside F-4.13-01's own description in this file), and F-4.13-01 is the record of that arm being unable to test the property at all. So the shipped product module now carries a dangling pointer AND the exact claim the phase already proved false, in the one place ebb62f4 did not correct it while correcting it in two others | open | This is the class the brief names: a confident comment is where the next reader stops checking. The docstring is otherwise excellent and that is what makes it dangerous, since a reader who has just been convinced by the correct NULL-semantics paragraph above it has no reason to doubt the paragraph below | 2026-08-27 judge: found by grepping the renamed arm across the repository after verifying F-4.13-01's fix. Finder does not close |
| F-4.13-J-02 | judge, by reading | minor | LATENT. `tests/system_03_search_agent/adapters/web_sse/test_phase_4_13_premise.py:486`, the failure message of the arm ebb62f4 corrected, still reads "history must read newest first, and the order must be total so rows sharing a timestamp do not shuffle". The docstring above it was fixed and now says the total order is NOT asserted here; the message a failing reader actually sees still claims it is. A person debugging a red arm reads the message, not the docstring | open | Cosmetic in isolation and filed only because it is the same overclaim as F-4.13-J-01 surviving in a second place, which makes it evidence about how the correction was applied rather than a defect on its own | 2026-08-27 judge: read off the arm while confirming F-4.13-01's fix. Finder does not close |
| F-4.13-J-03 | judge, by mutation | minor | LATENT, a gate gap rather than a product defect. `test_limit_is_a_real_sql_limit_and_returns_the_newest`, and the coverage bullet at `tests/system_03_search_agent/feedback/test_history.py:19` ("The `LIMIT` is real SQL, not a Python slice"), claim more than they prove. Mutation M6 replaced `.limit(limit)` with `.limit(MAX_LIMIT)` followed by a Python-side `[:limit]` and all 32 tests stayed green (`M6-python-slice-not-sql-limit: CAUGHT 0/1 \| 32 passed`). What the clause actually proves, and proves well, is that the limit is applied AFTER ordering, which its own parenthetical already says. The product code is correct; only the claim is too wide | open | The honest fix is a coverage-statement correction, not a new assertion: whether a Python slice is used is a static property of the source, exactly like the "no f-string builds any query string" property the same file already declares unprovable at runtime and delegates to reading and ruff. Declaring it the same way makes the gap arguable, which is what `goal-contracts` asks for | 2026-08-27 judge: measured with a 22-mutation sweep; this was one of three misses. Finder does not close |
| F-4.13-J-04 | judge, by mutation | minor | LATENT, a gate gap. The response-model bounds that `adapters/web_sse/app.py`'s own comment invokes by name ("`production-standards`' multi-agent pipeline gate applies here exactly as it does to every other response model on this surface: every string field carries `max_length`, and the list carries `max_length` too") are asserted by NO test. Three separate mutations stripped them and nothing went red: M20 removed every `max_length` from `HistoryItem`, M21 removed `max_length=MAX_LIMIT` from `HistoryResponse.items`, M22 removed `extra="forbid"`, and each returned `32 passed`. Confirmed by grep that no test in the repository names `HistoryItem` or `HistoryResponse` for its bounds, and that no repository-wide structural test over response models exists to catch it instead | open | The bounds are all PRESENT and correct today, so there is no reachable defect; what is missing is anything that keeps them present. `self-eval-loop` states the rule this breaks directly: where a comment asserts a security or correctness property, a test must assert the same property, or the comment is a liability rather than an aid. Note this composes with F-4.13-A-02: the `question` bound is the only thing standing between the product and a 500, and nothing would notice it being removed | 2026-08-27 judge: measured with a 22-mutation sweep; these were the other two of three misses, plus the `extra="forbid"` probe. Finder does not close |

## Coverage: what this phase does not cover

Stated here so a gap is arguable rather than invisible:

- Answers are not restored, only questions. See the scope boundary above.
- No pagination. The rail in `docs/build/design/design-system/prototype/app.html` is a flat scrolling list with no "load more" control, so the read path is capped at 50 rows and a person with more than 50 searches sees their 50 most recent. A cursor is a UI change first and a backend change second.
- Capture is best-effort by Section 16's own requirement, so a run whose capture row failed to write will not appear after a reload. This phase does not change that and does not disclose it in the UI.
- The rail is only rendered for a signed-in account (`railAvailable = signedIn && ...`, `frontend/src/App.tsx`), matching the prototype. The endpoint is still scoped for a guest principal, because the isolation property must hold whether or not any UI calls it.

## History

- 2026-08-27: phase opened, branch cut, six tickets decomposed. Substrate read first: `interactions.owner_id`, `_caller_owns_row`, `reassign_interaction_owner`, `get_caller`.
