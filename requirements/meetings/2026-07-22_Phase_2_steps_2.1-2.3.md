# Phase 2 working session: 2026-07-22 (Steps 2.1 and 2.3 core)

Date: 2026-07-22
Attendees: Monideep, Claude (agent teammate)
Type: competency questions and evaluation approach
Context: Phase 2 kickoff. Phase 1 is complete (75 decisions). This session covered Step 2.1 (reviewing the collected competency-question set) and the core of Step 2.3 (refining it: the moat test, scoring the Tier 1 set, and locking the v1 must-pass set).

## What we covered

Step 2.1: confirmed that `01_Consolidated_findings.md` is the already-collected user-research material (merged Confluence personas, journeys, roadmap, and Jira issues), not a pending scrape. Reviewed the set: 65 competency questions, three tiers (10 Tier 1, 28 Tier 2, 27 Tier 3), 11 personas, Tier 1 grouped by wedge type.

Step 2.3: built the selection bar (the moat test), scored the 10 Tier 1 questions against it, and locked the v1 must-pass set.

## Decisions (all logged in DECISIONS.md, 2026-07-22)

The moat test is the primary selection and tiering bar, above persona coverage and raw usage frequency.

It operates as a structured bar, not a flat score: provenance and deterministic are pass/fail gates; the one design-time ranking dimension is cannot-just-Google, scored by an external test (run the question against a general tool; if it cannot produce the cited answer, it passes); learn-from-the-system and loop-human-behavior defer to the Step 2.5 online loop; cross-database span was rejected as us-imposed bias. Deterministic means reproducible and verifiable given the data state at query time.

The PoC moat set is a deliberately small, cross-database-biased subset that proves the wedge, separate from the full set of queries the system handles.

Assemble-not-classify: moat CQs surface cited evidence and never render a clinical verdict (the Phase 1 forbidden-outputs boundary at the CQ level). Q1 and Q2 reworded to evidence-assembly.

No compute tools in v1: the three execution-heavy questions (Q2 VCF, Q7 SRA similarity, Q9 BLAST) defer to a fast-follow set, added later with pinned fixtures.

v1 must-pass moat set locked at seven: Q1 (reworded), Q3, Q4, Q5, Q6, Q8, Q10. Covers all three wedge types and personas 1, 2, 3, 4, 6, 8, 10, 11. Q1 carries a coordinate-range feasibility flag; drops to six if infeasible.

Two supporting decisions: no local MCP servers for inbound data (direct Python tools; MCP is an outbound delivery format only), and a full current-state deep dive of the NCBI, Datasets, and enrichment APIs added to Phase 4 as Step 4.0.

## Process

Built the `/phase-checkpoint` skill to sync planning docs at each boundary, registered in CLAUDE.md and backed by a memory. Parked thread: A/B test questions across model combinations (orchestrator plus planner), the online complement to model-bench, for the evaluation playbook and Phase 4.

## Action items

1. Claude: verify Q1's coordinate-range feasibility (dbVar and ClinVar by region, segmental-duplication overlap); if infeasible, Q1 drops to fast-follow and the cap is six.
2. Claude: define the coverage metric (concepts and predicates exercised versus available) and dispose of the Tier 2 and Tier 3 set under the new bar.
3. Claude: then Step 2.4 (offline eval gate, the 8-point rubric wired to eval-harness metrics) and Step 2.5 (online feedback loop).
4. Run `/ship` to commit and push this session's planning docs when ready.
