# Phase 4, steps 4.0 to 4.4: 2026-07-25

Date: 2026-07-25
Attendees: Monideep, Claude (agent teammate)
Type: full Phase 4 session, Step 4.0 through Step 4.4 (Phase 4 complete)
Context: Phase 4 opened 2026-07-24 with Step 4.0 next. This single session ran all of Phase 4: the API deep dive (4.0), the core-architecture decisions and the tech-spec outline, draft, reconciliation, and lock (4.1 to 4.3), and the strategic memo (4.4). Phase 4 is complete.

## What we covered

- API capability sheet:
    - Produced `requirements/phase_4/API_capability_sheet.md` (365 lines), live-verified against production NCBI and enrichment endpoints on 2026-07-25.
    - Scope: document every tool-touched database at its general capability surface, with the moat-seven competency questions as verification anchors, not the scope ceiling.
    - Layer 1 (the knowledge graph) trusted from its gate-verified server doc (2026-04-22); re-verified live in Phase 6.
- Feasibility flags, all three resolved, moat cap holds at seven:
    - Q1 (dbVar interval-overlap): resolved conditional, needs a two-step tool (ESearch prefilter plus placement-level post-filter).
    - Q5 (Pathogen Detection): resolved feasible via the FTP results tree (versioned PDG snapshots), no public JSON API exists.
    - Q6 (SRA metadata): resolved feasible via a two-tier access pattern (ESearch fields plus EFetch attributes).
- Reference repo mining: four Sonnet agents mined the Agentic-Search reference repo in parallel (API patterns, architecture, harness and caching, prior proposals); findings folded into the sheet.
- New rule: `.claude/rules/plan-then-fan-out.md` added to this repo and the owner's private personal operating system repository (reasoning model plans, cheaper models execute fan-out work).
- Config surface: `env.example` and `.env` updated with the full config surface the sheet surfaced. `.env` stays gitignored.
- Grading: the capability sheet was graded by a fresh-context agent per self-eval-loop.md; six fixes applied.
- Step 4.1 opened: core-outward architecture frame accepted (one agent core, a single service contract, UI/API/MCP/CLI as thin adapters). Layer 1 reachability from the deployed agent flagged as an open Step 4.1 decision.

## Decisions (logged in DECISIONS.md, 2026-07-25, now 102 rows)

- Step 4.0 verification approach: live-verify all Layer 2 and Layer 3 surfaces at general capability, moat-seven as anchors not ceiling; trust the gate-verified KG doc for Layer 1.
- Q1 resolved conditional: two-step coordinate-overlap tool, cap stays 7.
- Q5 access path: the FTP results tree, no public JSON API.
- Q6 resolved: two-tier ESearch plus EFetch access with attribute-name normalization.
- Layer 1 reachability from the deployed agent: open Step 4.1 decision, leaning read-only HTTPS query service.
- Step 4.1 architecture frame: build from the agent core outward.
- Added the plan-then-fan-out rule to both repos.

## Action items

1. Next: Phase 4 Step 4.1, outline the tech spec, starting with the Layer 1 reachability decision and the core-outward contract.
2. Claude: run `/ship` to commit and push the Step 4.0 closeout (capability sheet, plan-then-fan-out rule, decisions, planning-doc sync).

## Steps 4.1 to 4.4: architecture, tech spec, and memo

- Core architecture decided and logged (Decisions A, C, D, E, F, G plus a cost amendment, all 2026-07-25; DECISIONS.md is at 113 rows at Phase 4 close):
    - A: one agent core exposing a single typed event stream; the UI, REST plus SSE, GraphQL, MCP, KGX export, and CLI are thin adapters.
    - C: the coordinator-worker harness; the reader is scoped to untrusted free text.
    - D: Layer 1 transport-per-phase (SSH tunnel or co-locate for the prototype, a read-only HTTPS query service for v1, the database port never opens).
    - E: a deterministic trust signal (answer, flag, ask, refuse); the refuse path emits a pre-filled NCBI search link.
    - F: personalization lives in orchestration and memory, never in grounding; v1 ships a bounded-running-summary session memory.
    - G: the Phase 2 feedback loop stands; the interactions table is the shared substrate.
    - Cost amendment: the harness emits a cost event, and cost figures are builder-only.
- Tech spec (Steps 4.1 to 4.3): a 25-section outline, then all 25 sections drafted to implementation level by seven parallel Sonnet agents, reconciled into one canonical event and provenance schema, twice graded by fresh-context agents, and locked as the build contract (`requirements/Technical_specification.md`, locked 2026-07-25).
- Five open decisions confirmed by the user and applied: the acceptable-staleness threshold (graph-only, 30 and 90 days), the four provenance fields, the session-level A/B mechanism, the bounded-FIFO concurrency queue, and the tool-roster expansion to seven tools (added `pathogen_detection` for Q5 and `clinicaltrials_search` for Q4, with Datasets API v2 as an `ncbi_efetch` action).
- Delivery surfaces reconciled to the locked PRD: six surfaces.
- Strategic memo (Step 4.4): `requirements/Strategic_memo.md`, a one-read orientation for a future collaborator or future-me.
- Phase 4 is complete; the three deliverables (capability sheet, locked tech spec, strategic memo) serve as the phase synthesis.

## Action items (updated at Phase 4 close)

1. Phase 4 is complete. Next is Phase 5 (system and tooling updates): align bossman-mode, the skills, and the root documents to the locked tech spec's build order.
2. Two spec-flagged items carried to Phase 5: name an owner for the golden-fixture domain sign-off (Step 5.1), and clear the stale BioLink and KGX gates from the pull request template (Step 5.3).
