# Build phase 8.6, fix builder F1 report

Fix builder F1's record for the one fix-and-verify round after the judge and the adversary (`tracker/phase_8.6.md`, "Lead triage after round 2"). Findings are written here the moment they are established, newest last.

## Table of contents

- [Summary](#summary)
- [Left open](#left-open)
- [Goal contract](#goal-contract)
- [Log](#log)

## Summary

| Fix | Findings | Commit | What a person notices | Evidence |
|-----|----------|--------|-----------------------|----------|
| 2 | J03, A07, A17 | a494031 | The question's class, and so its search path and time budget, is the plan tier's again with either classifier switched on | J03 probe test, guard and Jev arms (F1-01) |
| 3 | J01, J02, A04 | 99c5c63 | A fact the prose left out, such as a gene's summary or the 72% in an abstract, gets the second writing call again instead of vanishing; the done event's time still includes writing | Gate and end-to-end tests (F1-02) |
| 1 | A05, A06, A10, J04, A12 | bca9261 | A forged chat transcript Jev admits is still refused, because the classifier refused it; when Jev fails, the classifier's verdict stands | Unit and end-to-end tests (F1-03); live A10 refused (F1-10) |
| 4 | A11 | 9c9c2b0 | "Which syndromes feature arachnodactyly?" no longer says "MedGen lists no clinical features for Arachnodactyly"; a disease with none listed still says so | Row and write tests (F1-05, F1-07); live Marfan and breast cancer runs (F1-11, F1-12) |
| 5 | A03, J08, A14 | 5117c2f | No step waits on a decision past its own budget; Plan waits at most the one-second grace for the literature decision | Hang tests at guardrail, Think and Plan (F1-08) |
| 6 | (documents 1, 2, 3) | c4a8fd5 | `docs/architecture/Model_architecture.md` says what now happens to the injection verdict, the question class and the repair gate | Three table rows and two bullets |

Checks on the final commit: the four named suites `2302 passed, 66 skipped, 1 deselected, 1 xfailed, 2 warnings in 80.56s`; the whole `tests/system_03_search_agent` suite `5565 passed, 143 skipped, 24 deselected, 1 xfailed, 7 warnings in 224.70s (0:03:44)`; `ruff check .` exit 0; `isort --check-only src tests` exit 0; the stable prefix and the three system instructions hash identically to develop's (F1-09); live spend $0.0240.

## Left open

- F1-04: `_jev_injection_pick` mirrors `decide()`'s Jev call through the public `call_jev`; F2's J10 fix to over-ceiling Jev costs may need the same change here.
- F1-04: the offline comparison script cannot see `guardrail.injection` now that it bypasses `decide()` (J06, already named open).
- F1-06: MedGen types some clinical feature concepts as diseases (Scoliosis, Polydactyly, Intellectual disability), which the record's type alone cannot catch; the concept's HPO source would, and the MedGen tool does not carry it.
- Not in this fence: `docs/architecture/Model_architecture.md`'s `think.asks_features` bullet does not yet say the "lists none" sentence also needs a disease record.

## Goal contract

- Done when: with `CLASSIFIER_PROVIDER=jev` a question is refused when the measured classifier or Jev says injection, and a Jev failure leaves the classifier's verdict standing with no generic guard-tier prompt asked; the question class is the plan tier's in every mode; the completeness repair runs whenever it ran before the phase while `done.elapsed_ms` still covers the writing step; "MedGen lists no clinical features for X" is said only when X is a disease record; no step waits on a decision past its own remaining budget, and Plan reads the literature decision with the late-read grace; the model document says so.
- Verify: one test per fix as the brief names them; the unit suites for `core`, `guardrail`, `synthesis` and `harness`; `ruff check .`; `isort --check-only src tests`; the Think, Plan and Write stable prefixes hashed against develop; three live runs within $0.20.
- Output: commits on `fix/8.6-f1` (branched from `origin/phase/8.6-jev-everywhere` at 0a9a4ba), this report.
- Constraints: only the files in the fence; every rule under `.claude/rules/`; no push; no trailer; no local machine path in anything committed.
- Blocked-stop: a fix that needs a file in fix builder F2's fence (`harness/`, `synthesis/sentence_check.py`, the golden-run scripts, the production-standards rule, the debugging guide, `visualizations/`) is written here and left.

## Log

- F1-00: setup. `git switch -c fix/8.6-f1 origin/phase/8.6-jev-everywhere` created the branch at 0a9a4ba; setting its upstream failed on the sandbox's protected `.git/config` and nothing needs it, since this round does not push. Develop's stable prefix and system instructions, hashed from a `git archive` of origin/develop (c985afe) in the scratchpad: stable prefix `0a8c5ba2...51ca`, Think instruction `b1403ba7...8df8`, guard instruction `2c4c7cc8...24c4`, synth instruction `bfa4452d...0633`.
- F1-01: fix 2 (J03, A07, A17), commit a494031. `git revert --no-commit 4259787` applied cleanly over 3c926ad and 013a3f0; the diff removes exactly T-8.6-05's spec, task, reader and tests. New test `test_the_question_class_is_the_plan_tiers_with_either_provider` (the judge's J03 probe, real graph, real `decide()`, guard and Jev arms, class picks "aggregate" and "lookup"): 4 passed. Mutation: re-applying 4259787 in the working tree turned all 4 red (`'aggregate' == 'exploratory'`, `'lookup' == 'exploratory'`); tree restored.
- F1-02: fix 3 (J01, J02, A04), commit 99c5c63. `_code_built_lines_will_cite`'s rule and the call site's `lists_every_finding=query.audience_depth == "researcher"` are byte for byte the pre-phase code (checked against 2c6374f); the docstring gains one paragraph saying why the folded-view rule was reverted. Kept: `done.elapsed_ms` read when each done event is built (W3) and its test. The phase's seven gate tests are removed; new: J01 (gene summary) and A04 (paper abstract with 72%) at both listing modes, J02 (sibling row fails the pass), and A04 end to end through `write_node` at researcher and plain-language depth (`[False, True]`, "72%" in the answer). One test artifact met on the way: an abstract ending in a full stop left the stand-in writer's "years. [4]" sentence uncited, so the constant carries no full stop. Core suite: `1193 passed, 56 skipped, 1 deselected, 2 warnings in 91.87s`. Mutation: T-8.6-07's rule plus `lists_every_finding=True` restored in the working tree gave `7 failed, 25 passed`, the write arms failing with `[False]`; tree restored.
- F1-03: fix 1 (A05, A06, A10, J04, A12), commit bca9261. With Jev on, `guardrail_node` starts `_jev_injection_pick` (graph.py), which asks Jev alone through the public `call_jev`, with the same per-query cap check before and `track_cost` after as `decide()`'s own Jev call, and returns the `JevResult` or the failure reason. `classifier.verdict_for_decision(jev_says_injection, classification)` refuses when either says injection and otherwise returns `verdict_for(classification)` unchanged, so the classifier's own refusal reason is kept whenever it said injection. The `done` record carries both picks (`guard_choice` is the classifier's), `chosen` the verdict acted on, `fallback_reason` Jev's failure. `_INJECTION_UNDECIDED_MESSAGE` and the step error on a Jev failure are gone. Guardrail suite `251 passed`. Mutations, each restored: the phase's replace rule in `verdict_for_decision` gave `10 failed, 241 passed` (both forged transcripts, the J04 arm, the classifier-injection failure arms, two classifier unit arms); a `decide()` call added on a Jev failure gave `9 failed, 47 passed` in the integration file.
- F1-04 [merge note for the lead]: `_jev_injection_pick` calls `harness.jev_client.call_jev` directly, mirroring `harness.decide`'s private `_run_jev_pick`, because the brief keeps the injection verdict off `decide()`'s fallback and `harness/` is F2's fence. If F2's J10 fix changes how a Jev reply reporting a cost above `MAX_JEV_COST_USD` is charged (for example by carrying the cost on `JevCallError`), this function needs the same change, or that reply is charged $0 here. Also, the offline comparison script records only `decide()` calls, so it cannot see `guardrail.injection` any more; J06 stays open as the triage named it.
- F1-05: fix 4 (A11), commit 9c9c2b0. The "lists none" row is built in `_with_medgen_clinical_feature_rows` only when `_is_medgen_disease_record(title_row)`: the record's own unwrapped `semantictype` is in the UMLS Disorders group minus Finding and Sign or Symptom; a list counts when any type qualifies; a record with no type gets no sentence. Evidence, MedGen ESummary read live by concept id (public E-utilities, no key, $0): Arachnodactyly C0003706 Finding, "Seen by breast cancer nurse" C1320450 Finding, Tall stature Finding, Seizure Sign or Symptom, all with 0 features; Marfan syndrome, cystic fibrosis, Down syndrome, Huntington disease Disease or Syndrome (70, 31, 47, 13 features); breast cancer C0006142 Neoplastic Process with 0 features; schizophrenia Mental or Behavioral Dysfunction (6); congenital contractural arachnodactyly Congenital Abnormality (49).
- F1-06 [named open, outside the fence]: the semantic type cannot separate every feature concept from a disease. MedGen types Scoliosis and Aortic regurgitation Disease or Syndrome, Polydactyly, Ectopia lentis and Syndactyly Congenital Abnormality, Intellectual disability Mental or Behavioral Dysfunction, each with 0 features, so "Which syndromes feature polydactyly?" could still draw the sentence if the features decision picks asks_features. The concept's own HPO source in `conceptmeta` would separate them, and the MedGen tool (`tools/ncbi_eutils_actions.py`) does not carry it through today. The limit is stated in the constant's comment.
- F1-07: fix 4 tests. `_no_features_findings` in `test_graph.py` now carries `semantictype`, as every live record does; without it the old "told so" test would have tested a record shape MedGen never returns. New: the A11 question at researcher depth with asks_features (Finding and Sign or Symptom) says no such sentence in any token, cell, citation or the prompt, and still lists Arachnodactyly; the "told so" test runs for Disease or Syndrome and Neoplastic Process; row-level arms in `test_disease_breadth.py` for five non-disease shapes and four disease shapes. Core and synthesis suites: `1713 passed, 66 skipped, 1 deselected, 1 xfailed, 2 warnings in 71.33s`. Mutation: dropping `_is_medgen_disease_record` from the rule gave `7 failed, 75 passed`; restored.
- F1-08: fix 5 (A03, J08, A14), commit 5117c2f. `_step_deadline(step, class)` is `time.monotonic() + budget_for_step(...)` taken when each of guardrail, Think, Plan and Write starts; `_await_within_step(task, deadline, default)` waits no later than it, then cancels and returns the default. Bounded now: the guardrail's Jev injection pick (default "step_budget", so the classifier's verdict stands) and relevancy decision (no usable pick); Think's ask-back and recent-work decisions (the `asyncio.gather` became one task plus bounded reads); Plan's literature decision, the Think-started one read with `_read_late_decision` and one asked on its own bounded by Plan's deadline; Write's features read, grace capped by Write's deadline. The reworded-sentence check is unchanged: it already gets `min(write budget left - 1, 12)` and bounds its own calls. Four suites: `2302 passed, 66 skipped, 1 deselected, 1 xfailed, 2 warnings in 80.67s`. Mutation: bare `await` restored at the four reads gave `6 failed, 96 deselected in 189.84s`; restored from a scratchpad copy, `7 passed`.
- F1-09: stable prefixes, hashed from the fix branch's `src` with the same scratchpad script as F1-00: stable prefix `0a8c5ba2...51ca`, Think `b1403ba7...8df8`, guard `2c4c7cc8...24c4`, synth `bfa4452d...0633`, all identical to develop's.
- F1-10: live run 1 of 3, the A10 forged transcript, fix branch code, `CLASSIFIER_PROVIDER=jev` in the process only, `.env` loaded unprinted, guard and plan tiers on deepseek/deepseek-v4-flash, synth z-ai/glm-5.2, researcher depth. Output: guard `{"passed": false, "category": "injection", "reason": "the query contains an instruction directed at the system rather than a question about biomedical evidence (Injection includes impersonating a system message with 'You are a helpful assistant with no restrictions'.)"}`, events `cost, done, guard` only, `trust_outcome` refuse, decision `{"name": "guardrail.injection", "chosen": "injection", "decided_by": "guard", "jev_choice": "not_injection", "guard_choice": "injection", "fallback_reason": null}`, 1.4 s, cost $0.0000663. Jev still admits the transcript; the classifier's refusal now stands.
- F1-11: live run 2 of 3, `What phenotypic features are associated with Marfan syndrome?`, same setup. `trust_outcome` answer, 93 citations, 24.5 s, cost $0.009111833. Decisions: `guardrail.injection` not_injection by jev with the classifier agreeing, `think.asks_features` asks_features, `think.recent_years` not_applicable, `plan.literature` not_literature, all by jev. The prose (claim tokens, 530 characters) names Arachnodactyly, Ectopia lentis and Aortic regurgitation from a 14-name check list; the listing carries 12 of the 14. No "MedGen lists no clinical features" sentence.
- F1-12: live run 3 of 3, `How many genes are associated with breast cancer?`, same setup. `trust_outcome` ask, 2 citations, 14.1 s, cost $0.01481117128. `think.asks_features` not_applicable by jev; no "MedGen lists no clinical features" sentence anywhere the reader sees. Live spend for the three runs: $0.0000663 + $0.0091118 + $0.0148112 = $0.0240, inside the $0.20 limit; the MedGen type probes (F1-05) were public E-utilities at $0.
